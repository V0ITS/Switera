---
phase: 01-admin-city-stock-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ManajemenKota.jsx
  - src/components/Layout.jsx
  - src/utils/navigation.js
  - src/App.jsx
autonomous: false
requirements: [ADMIN-01, ADMIN-05]
tags: [crud, react, brownfield, admin]

must_haves:
  truths:
    - "Admin sees a new 'Manajemen Kota' menu item in the sidebar (Admin role only)"
    - "Clicking 'Manajemen Kota' renders the new page, not a silent Dashboard fallback"
    - "Admin sees a table listing all cities with their nama and kapasitas (ton)"
    - "Admin sees a TBS stock summary card showing the current stokTbs value"
    - "Manajer Distribusi and Tim Logistik do NOT see the 'Manajemen Kota' menu item"
  artifacts:
    - path: "src/pages/ManajemenKota.jsx"
      provides: "Manajemen Kota page: snapshot-subscribe, city table, stock summary card"
      min_lines: 60
    - path: "src/components/Layout.jsx"
      provides: "New 'city' case in IkonMenu switch"
      contains: 'case "city"'
    - path: "src/utils/navigation.js"
      provides: "menuByRole.Admin entry for manajemen-kota"
      contains: '"manajemen-kota"'
    - path: "src/App.jsx"
      provides: "pageRegistry + pathByPage entries for manajemen-kota"
      contains: '"manajemen-kota"'
  key_links:
    - from: "src/utils/navigation.js"
      to: "src/App.jsx"
      via: "menuByRole.Admin key 'manajemen-kota' resolves to pageRegistry['manajemen-kota']"
      pattern: "manajemen-kota"
    - from: "src/pages/ManajemenKota.jsx"
      to: "src/store.js"
      via: "snapshot-subscribe reads snapshot.daftarKota and snapshot.stokTbs"
      pattern: "store\\.subscribe"
---

<objective>
Deliver the thinnest end-to-end vertical slice of the "Manajemen Kota" feature: a new Admin-only page that is reachable from the sidebar and renders the existing city list (ADMIN-01) plus a read-only TBS stock summary card (read side of ADMIN-05). No mutations yet — this slice proves the page exists, is routed correctly, is role-gated, and reads live store state reactively.

Purpose: Establish the page + routing + navigation + icon scaffold as a working vertical slice so that after this plan an Admin can navigate to and SEE the management surface. Add/edit/delete and stock editing build on this page in plans 02 and 03.
Output: New `src/pages/ManajemenKota.jsx`; additive edits to `Layout.jsx` (new icon), `navigation.js` (menu entry), `App.jsx` (route registration).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-city-stock-management/01-CONTEXT.md
@.planning/phases/01-admin-city-stock-management/01-RESEARCH.md
@.planning/phases/01-admin-city-stock-management/01-PATTERNS.md
@.planning/phases/01-admin-city-stock-management/01-UI-SPEC.md

# Structural analog to copy — read fully before writing the new page
@src/pages/ManajemenData.jsx
# Shared components reused by the new page
@src/components/MetricCard.jsx
@src/components/PageHeader.jsx
@src/components/SectionHeader.jsx
</context>

## Phase Goal (User Story)

**As an** Admin, **I want to** manage the city list and TBS stock level through a dedicated page, **so that** city data and stock stay accurate and propagate everywhere without any silent data-integrity gaps.

<tasks>

<task type="auto">
  <name>Task 1: Register the new "Manajemen Kota" page across all three registries + add the city icon</name>
  <files>src/App.jsx, src/utils/navigation.js, src/components/Layout.jsx</files>
  <read_first>
    - src/App.jsx (lines 1-45 — page imports, pageRegistry, pathByPage, pageByPath derivation)
    - src/utils/navigation.js (full file — menuByRole.Admin array)
    - src/components/Layout.jsx (the IkonMenu switch, ~lines 114-204 — existing icon cases like "database", "report")
    - .planning/phases/01-admin-city-stock-management/01-PATTERNS.md (sections for App.jsx, navigation.js, Layout.jsx — exact additive edits)
  </read_first>
  <action>
    Make all registry edits in ONE task/commit to avoid Pitfall 4 (split edits cause App.jsx:151 `pageRegistry[activePage] ?? Dashboard` to silently render Dashboard instead of the new page). Per D-03:
    1. In src/App.jsx: add `import ManajemenKota from "./pages/ManajemenKota";` alongside the other page imports; add `"manajemen-kota": ManajemenKota,` to the `pageRegistry` object and `"manajemen-kota": "/manajemen-kota",` to the `pathByPage` object. Do NOT edit `pageByPath` (it is auto-derived from `pathByPage`). Do NOT touch the four routing useEffects.
    2. In src/utils/navigation.js: insert `{ key: "manajemen-kota", label: "Manajemen Kota", icon: "city" }` into the `menuByRole.Admin` array, positioned after the `manajemen-data` entry and before `riwayat-aktivitas`. Do NOT modify the "Manajer Distribusi" or "Tim Logistik" arrays — this is an Admin-only page.
    3. In src/components/Layout.jsx: add a new `case "city":` to the IkonMenu switch, returning an SVG with `viewBox="0 0 24 24" aria-hidden="true" style={iconStyle}` and two strokes at `strokeWidth="1.5"` forming a simple building/house glyph (e.g. an outer roof+walls path and an inner door path, using `stroke={color}` with no fill, matching the structure of the existing `"database"` case). Do NOT reuse the `"database"` glyph — it is already used by "Manajemen Data" and would be visually ambiguous. Since `src/pages/ManajemenKota.jsx` does not exist until Task 2, create Task 2's file first OR create a minimal placeholder default export so the import resolves; the executor should complete Task 2 in the same plan run so the import is satisfied at build time.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const nav=fs.readFileSync('src/utils/navigation.js','utf8');const app=fs.readFileSync('src/App.jsx','utf8');const lay=fs.readFileSync('src/components/Layout.jsx','utf8');if(!/manajemen-kota/.test(nav))throw new Error('navigation.js missing manajemen-kota');if(!/manajemen-kota[\"']?\s*:\s*ManajemenKota/.test(app))throw new Error('App.jsx pageRegistry missing');if(!/[\"']manajemen-kota[\"']\s*:\s*[\"']\/manajemen-kota/.test(app))throw new Error('App.jsx pathByPage missing');if(!/case\s+[\"']city[\"']/.test(lay))throw new Error('Layout.jsx missing city icon case');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.jsx` imports `ManajemenKota` and has `manajemen-kota` keys in BOTH `pageRegistry` and `pathByPage`.
    - `src/utils/navigation.js` `menuByRole.Admin` contains `{ key: "manajemen-kota", label: "Manajemen Kota", icon: "city" }` and the other two role arrays are unchanged.
    - `src/components/Layout.jsx` IkonMenu has a `case "city"` returning a two-stroke SVG distinct from `"database"`.
    - `npm run build` succeeds (import resolves to the page created in Task 2).
  </acceptance_criteria>
  <done>Logging in as Admin shows a "Manajemen Kota" sidebar item with a city icon; clicking it renders the ManajemenKota page (not Dashboard). The item is absent for Manajer Distribusi and Tim Logistik.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ManajemenKota.jsx — snapshot-subscribe, city list table (ADMIN-01), stock summary card (ADMIN-05 read side)</name>
  <files>src/pages/ManajemenKota.jsx</files>
  <read_first>
    - src/pages/ManajemenData.jsx (FULL file — structural analog: imports, snapshot-subscribe at ~line 60/71-77, AksiTabelButtons at lines 23-57, render tree at lines 336-413, Tabel/EmptyState usage)
    - src/store.js (lines 235-284 — getDaftarKota, getKapasitasKota, getStokTbs/setStokTbs contract; cities keyed by `nama` string, NO id field)
    - src/components/MetricCard.jsx (FULL — size="lg" variant, label/nilai props, children slot for the Edit button)
    - src/components/Tabel.jsx (kolom/data/aksi props, numeric column flag)
    - src/components/PageHeader.jsx and src/components/SectionHeader.jsx (judul/deskripsi/aksi slot shapes)
    - src/components/EmptyState.jsx (pesan prop)
    - .planning/phases/01-admin-city-stock-management/01-UI-SPEC.md (Copywriting Contract — exact title/labels/empty-state copy; Interaction & Layout Notes — page layout zones, table columns)
    - .planning/phases/01-admin-city-stock-management/01-PATTERNS.md (ManajemenKota.jsx pattern assignments — imports, snapshot-subscribe, AksiTabelButtons, render tree)
  </read_first>
  <action>
    Create `src/pages/ManajemenKota.jsx` as a function-declaration component `ManajemenKota({ onNavigate })` with `export default ManajemenKota` at the bottom, copying the structural shape of `ManajemenData.jsx`. This task delivers the READ-ONLY slice; do NOT wire add/edit/delete/stock-edit modals yet (those are plans 02 and 03), but DO lay out the page so those zones exist.
    1. Imports: `useEffect, useMemo, useState` from react; `Card`, `EmptyState`, `Modal`, `PageHeader`, `SectionHeader`, `Tabel`, `Tombol`, `MetricCard` from `../components/*`; `store` from `../store`. (Import `Modal`/`Tombol` now even if the modals are wired later, to keep the file stable across plans.)
    2. Snapshot-subscribe exactly as the analog: `const [snapshot, setSnapshot] = useState(store.getState());` and `useEffect(() => { const unsubscribe = store.subscribe(setSnapshot); return unsubscribe; }, []);`.
    3. Derive view data FROM the snapshot (never call store getters in render — Pitfall 5): `const daftarKota = useMemo(() => snapshot.daftarKota ?? [], [snapshot.daftarKota]);` and `const stokTbs = snapshot.stokTbs ?? 0;`.
    4. Render `PageHeader` with title "Manajemen Kota" and description "Kelola daftar kota dan kapasitas, serta stok TBS tersedia." The PageHeader `aksi` slot should hold a "+ Tambah Kota" `Tombol` (variant primer) whose onClick is a no-op placeholder this plan (wired in plan 02) — render it so the layout is correct.
    5. Render a TBS stock summary `MetricCard` with `label="Stok TBS Tersedia (ton)"`, `nilai={\`${stokTbs} ton\`}`, `size="lg"`, `accent="primary"`, containing a placeholder `Tombol label="Edit" variant="sekunder"` (onClick no-op this plan; wired in plan 02). Position it above the city table per D-04; single full-width stacked Card is acceptable (Claude's Discretion).
    6. Render the city table inside a `Card` + `SectionHeader` (label like "Daftar Kota — Menampilkan {daftarKota.length} kota"): if `daftarKota.length > 0` render `Tabel` with columns `nama` (label "Nama Kota", text, sortable) and `kapasitas` (label "Kapasitas (ton)", numeric: true, mono, sortable), plus an `aksi` render-prop showing Edit/Hapus buttons (copy the `AksiTabelButtons` presentational component verbatim from ManajemenData.jsx lines 23-57; its onEdit/onDelete may be no-op placeholders this plan). If `daftarKota.length === 0` render `EmptyState` with pesan "Belum ada kota yang terdaftar. Tambahkan kota pertama untuk mulai mengatur kapasitas dan distribusi TBS."
    7. Do NOT invent an `id` field for cities and do NOT call `getNextId` — cities are keyed by `nama` (string) only.
    8. Use `var(--space-N)` token variables / token-driven shared components for any new spacing; do not hardcode pixel values.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/ManajemenKota.jsx','utf8');if(!/export default ManajemenKota/.test(s))throw new Error('missing default export');if(!/store\.subscribe/.test(s))throw new Error('missing snapshot-subscribe');if(!/snapshot\.daftarKota/.test(s))throw new Error('not reading daftarKota from snapshot');if(!/snapshot\.stokTbs/.test(s))throw new Error('not reading stokTbs from snapshot');if(/getNextId/.test(s))throw new Error('cities must not use getNextId');if(/store\.getStokTbs\(\)/.test(s))throw new Error('must read stokTbs from snapshot, not getStokTbs() in render');if(s.split('\n').length<60)throw new Error('file too short, likely incomplete');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/ManajemenKota.jsx` exists, default-exports `ManajemenKota`, and is ≥60 lines.
    - The component subscribes to the store and reads `daftarKota` and `stokTbs` from the subscribed `snapshot` (verified: no `store.getStokTbs()` call inside render, no `getNextId`, no `kota.id`).
    - `npm run build` succeeds.
    - Page title text is "Manajemen Kota"; stock card label is "Stok TBS Tersedia (ton)"; table columns are "Nama Kota" and "Kapasitas (ton)".
  </acceptance_criteria>
  <done>Logging in as Admin and opening "Manajemen Kota" shows a table of all 8 seed cities with correct kapasitas values and a stock summary card showing the current stokTbs (seed 150 ton). No console errors. Table/card update reactively if store state changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — reachable Admin-only Manajemen Kota page with city list and stock card</name>
  <action>Pause for human verification. Do not proceed until the verifier approves. Present the steps in how-to-verify and wait for the resume-signal.</action>
  <what-built>The new Admin-only "Manajemen Kota" page: sidebar entry + city icon + route registration, plus a read-only city table (ADMIN-01) and a TBS stock summary card (ADMIN-05 read side). No mutations are wired yet.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a browser.
    2. Log in as Admin (username `admin`, password `admin123`).
    3. Confirm a "Manajemen Kota" item appears in the sidebar with a city/building icon distinct from "Manajemen Data".
    4. Click it. Confirm the Manajemen Kota page renders (NOT the Dashboard) at path `/manajemen-kota`.
    5. Confirm the city table lists all 8 seed cities (Pekanbaru, Medan, Palembang, Jambi, Padang, Dumai, Bengkalis, Rokan Hilir) with their kapasitas values, and the TBS stock card shows the current stock (seed: 150 ton).
    6. Log out and log in as Manajer Distribusi, then as Tim Logistik. Confirm "Manajemen Kota" does NOT appear in the sidebar for either role, and no redirect loop / blank page occurs.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe what's wrong (wrong page renders, icon missing, item visible to non-Admin, table empty/incorrect).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin user → page render | City names from store state are rendered as text into JSX |
| Role gating (UI) → page access | menuByRole + App.jsx allowedPages decide which page an authenticated user can render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | New Admin-only page exposed to non-Admin roles | mitigate | Register only in `menuByRole.Admin`; do NOT add to the other two role arrays. UI-level gating consistent with every other page (not a real authz boundary — accepted app-wide characteristic). |
| T-01-02 | Tampering (Stored XSS) | `{kota.nama}` rendered in the table | mitigate | Render city name only as JSX text content (React auto-escapes). Never via `dangerouslySetInnerHTML`, never built into a `style`/`href` string by concatenation. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (RESEARCH.md Package Legitimacy Audit: "Not applicable"). No install task exists. Any `npm install` here signals scope drift and must be rejected. |
</threat_model>

<verification>
- `npm run build` succeeds with the new page imported and registered.
- Automated structural checks (Task 1 + Task 2 verify commands) pass.
- Manual UAT: ADMIN-01 (city list visible) and ADMIN-05 read side (stock value visible) per 01-VALIDATION.md Manual-Only Verifications table.
- Role-isolation: "Manajemen Kota" visible only to Admin.
</verification>

<success_criteria>
- Admin can navigate to and view the Manajemen Kota page.
- All cities render with correct kapasitas; stock card shows current stokTbs.
- Page reads live store state via snapshot-subscribe (reactive, no manual refresh).
- Non-Admin roles never see the menu item.
</success_criteria>

<output>
Create `.planning/phases/01-admin-city-stock-management/01-01-SUMMARY.md` when done.
</output>
