---
phase: 01-admin-city-stock-management
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pages/ManajemenKota.jsx
autonomous: false
requirements: [ADMIN-02, ADMIN-06, ADMIN-05]
tags: [crud, react, validation, admin]

must_haves:
  truths:
    - "Admin can open an 'Tambah Kota Baru' modal, fill nama + kapasitas, and save a new city"
    - "Submitting a blank name shows 'Nama kota wajib diisi.' inline under the Nama Kota field (not a toast)"
    - "Submitting a non-positive capacity shows 'Kapasitas harus berupa angka positif.' inline"
    - "Submitting a duplicate city name shows 'Kota dengan nama tersebut sudah ada.' inline under Nama Kota (not a toast), modal stays open, no crash"
    - "Admin can edit an existing city's kapasitas and the table updates immediately"
    - "Admin can open 'Perbarui Stok TBS' modal, change the value, save, and the stock card updates immediately"
  artifacts:
    - path: "src/pages/ManajemenKota.jsx"
      provides: "Add/edit-city form modal with inline validation + thrown-error surfacing; stock-edit modal"
      contains: "validateForm"
  key_links:
    - from: "src/pages/ManajemenKota.jsx"
      to: "src/store.js"
      via: "submit calls store.tambahKota / store.updateKota; stock modal calls store.setStokTbs"
      pattern: "store\\.(tambahKota|setStokTbs)"
    - from: "src/pages/ManajemenKota.jsx (catch block)"
      to: "formErrors.nama"
      via: "tambahKota thrown Error surfaced as setFormErrors({ nama: error.message })"
      pattern: "setFormErrors"
---

<objective>
Add the create/update vertical slice to the Manajemen Kota page built in plan 01: a working add-city form with inline validation and duplicate-name handling (ADMIN-02, ADMIN-06), capacity-only editing of existing cities (the non-rename part of ADMIN-03), and the TBS stock editor (write side of ADMIN-05). All mutations use the already-complete store mutators (`tambahKota`, `updateKota`, `setStokTbs`) — no store.js changes in this plan.

Purpose: After this plan an Admin can actually create cities, fix capacities, and update stock — capabilities they could not perform after plan 01 (which only rendered read-only views). Rename-cascade and delete are deliberately deferred to plan 03 because they require store-layer data-integrity logic.
Output: Extended `src/pages/ManajemenKota.jsx` with the add/edit form modal, validation, error surfacing, and stock-edit modal wired to real store mutators.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-city-stock-management/01-CONTEXT.md
@.planning/phases/01-admin-city-stock-management/01-RESEARCH.md
@.planning/phases/01-admin-city-stock-management/01-PATTERNS.md
@.planning/phases/01-admin-city-stock-management/01-UI-SPEC.md

# The page created in plan 01 (this plan extends it)
@src/pages/ManajemenKota.jsx
# Validation pattern analog
@src/pages/InputData.jsx
# Store mutator contract
@src/store.js
</context>

## Phase Goal (User Story)

**As an** Admin, **I want to** manage the city list and TBS stock level through a dedicated page, **so that** city data and stock stay accurate and propagate everywhere without any silent data-integrity gaps.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add-city + capacity-edit form modal with inline validation (ADMIN-02) and duplicate-name surfacing (ADMIN-06)</name>
  <files>src/pages/ManajemenKota.jsx</files>
  <read_first>
    - src/pages/ManajemenKota.jsx (the page from plan 01 — current placeholder "+ Tambah Kota" / Edit handlers to wire up)
    - src/pages/InputData.jsx (lines ~51-118 — validate(form) -> {field:message} shape, called on change AND before submit)
    - src/pages/ManajemenData.jsx (lines ~144-186 — openEditModal / saveEdit shape; Modal usage at ~415-515)
    - src/store.js (lines 247-265 — tambahKota throws "Kota dengan nama tersebut sudah ada." on duplicate; updateKota signature updateKota(namaLama, {nama, kapasitas}))
    - .planning/phases/01-admin-city-stock-management/01-UI-SPEC.md (Copywriting Contract — modal titles, field labels, error strings, success toasts)
    - .planning/phases/01-admin-city-stock-management/01-PATTERNS.md (validate+submit+catch-thrown-error pattern, Modal usage pattern)
  </read_first>
  <action>
    Add local state to the page: `form` ({ nama, kapasitas }), `formErrors` ({}), `editTarget` (null = add mode, city object = edit mode), `isFormOpen` (bool). Add an `initialForm = { nama: "", kapasitas: "" }` module constant.
    1. `validateForm(nextForm)` returns a `{field: message}` object per D-06: if `!nextForm.nama.trim()` set `nextErrors.nama = "Nama kota wajib diisi."`; if `!nextForm.kapasitas || Number(nextForm.kapasitas) <= 0` set `nextErrors.kapasitas = "Kapasitas harus berupa angka positif."`. Do NOT add any other validation (D-06: exactly these two). Do NOT re-implement a page-local duplicate-name check — that is delegated to the store's thrown Error (anti-pattern to duplicate it).
    2. `openAddModal()`: setEditTarget(null), setForm(initialForm), setFormErrors({}), setIsFormOpen(true). Wire this to the existing "+ Tambah Kota" Tombol in PageHeader (replacing the plan-01 no-op).
    3. `openEditModal(kota)`: setEditTarget(kota), setForm({ nama: kota.nama, kapasitas: String(kota.kapasitas) }), setFormErrors({}), setIsFormOpen(true). Wire this to the table row Edit action (AksiTabelButtons onEdit).
    4. `submitForm()`: compute `validateForm(form)`; if it has keys, setFormErrors and return. Otherwise wrap in try/catch: if `editTarget`, call `store.updateKota(editTarget.nama, { nama: form.nama.trim(), kapasitas: form.kapasitas })` then showToast success "Kota berhasil diperbarui."; else call `store.tambahKota({ nama: form.nama.trim(), kapasitas: form.kapasitas })` then showToast success "Kota berhasil ditambahkan." On success close the modal and reset form/errors/editTarget. In `catch (error)`, set the inline field error per D-05: `setFormErrors({ nama: error.message })` — NEVER a toast and NEVER console.error for this case. (tambahKota is the one store mutator that throws; updateKota does not throw in this plan since rename-cascade is plan 03 and capacity-only edits keep the same nama.)
    5. Render the add/edit `Modal`: title "Tambah Kota Baru" when adding, "Edit Kota" when editing. Two fields: "Nama Kota" (text) and "Kapasitas (ton)" (numeric). Render each field's error as a `<p>` styled with the danger color directly under the offending field (match ManajemenData.jsx `errorStyle` precedent), recomputing validation on each change. Buttons: "Batal" (sekunder, closes without saving) and a primer submit button ("Simpan Perubahan" or equivalent) calling submitForm.
    6. Recompute `validateForm` on each `handleChange` so errors clear as the user fixes them (matching InputData.jsx behavior).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/ManajemenKota.jsx','utf8');if(!/validateForm/.test(s))throw new Error('missing validateForm');if(!/store\.tambahKota/.test(s))throw new Error('missing tambahKota call');if(!/setFormErrors\(\s*\{\s*nama:\s*error\.message/.test(s))throw new Error('duplicate-name error must be surfaced as setFormErrors({nama: error.message})');if(!/Nama kota wajib diisi\./.test(s))throw new Error('missing required-name error string');if(!/Kapasitas harus berupa angka positif\./.test(s))throw new Error('missing capacity error string');if(/daftarKota\.some\(/.test(s))throw new Error('do not re-implement page-local duplicate check; delegate to store thrown Error');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Page has `validateForm` returning `{nama?, kapasitas?}` with exactly the two D-06 checks; no third validation, no page-local duplicate check.
    - Add path calls `store.tambahKota`; edit path calls `store.updateKota(editTarget.nama, {...})`.
    - The `catch` block surfaces the thrown duplicate-name message via `setFormErrors({ nama: error.message })`, not a toast, not console.error.
    - Modal titles/labels/error strings match the UI-SPEC Copywriting Contract verbatim.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>As Admin: "+ Tambah Kota" opens the modal; blank name shows "Nama kota wajib diisi." inline; a duplicate name (e.g. "Medan") shows "Kota dengan nama tersebut sudah ada." inline under Nama Kota with the modal staying open and no crash; a valid new city saves with a success toast and appears in the table. Editing an existing city's capacity updates the table immediately.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TBS stock-edit modal wired to store.setStokTbs (ADMIN-05 write side, D-04)</name>
  <files>src/pages/ManajemenKota.jsx</files>
  <read_first>
    - src/pages/ManajemenKota.jsx (the stock MetricCard + placeholder Edit button from plan 01 / Task 1 state above)
    - src/store.js (lines 274-284 — setStokTbs(value) coerces Number(value)||0, records activity, notifies once)
    - src/components/Modal.jsx (judul / onTutup / konten props)
    - .planning/phases/01-admin-city-stock-management/01-UI-SPEC.md (TBS stock edit interaction note — card-with-edit-button, single numeric field, modal title "Perbarui Stok TBS", error "Stok harus berupa angka positif.")
  </read_first>
  <action>
    Wire the stock editor per D-04 (card-with-edit-button opening a modal, NOT inline edit, NOT a table row). Add state: `isStockModalOpen` (bool), `stockValue` (string), `stockError` (string).
    1. `openStockModal()`: setStockValue(String(snapshot.stokTbs ?? 0)), setStockError(""), setIsStockModalOpen(true). Wire it to the existing stock-card "Edit" Tombol from plan 01 (replacing the no-op).
    2. `submitStock()`: parse `const numericValue = Number(stockValue);` if `!stockValue || numericValue <= 0` set stockError "Stok harus berupa angka positif." and return. Otherwise call `store.setStokTbs(numericValue)`, close the modal, and showToast success "Stok TBS berhasil diperbarui."
    3. Render the stock-edit `Modal` (title "Perbarui Stok TBS") with one numeric field labeled "Stok TBS Tersedia (ton)" pre-filled with the current value, the `stockError` rendered inline under it in danger color, and "Batal" (sekunder) / "Simpan" (primer) buttons.
    4. The stock summary card must keep reading `snapshot.stokTbs` (never cache `store.getStokTbs()` in local state) so it updates immediately after save — this is what makes the value propagate to the recommendation engine via the existing snapshot-subscribe path with zero extra wiring (Pitfall 5).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/pages/ManajemenKota.jsx','utf8');if(!/store\.setStokTbs/.test(s))throw new Error('missing setStokTbs call');if(!/Stok harus berupa angka positif\./.test(s))throw new Error('missing stock validation error string');if(!/Stok TBS berhasil diperbarui\./.test(s))throw new Error('missing stock success toast');if(!/Perbarui Stok TBS/.test(s))throw new Error('missing stock modal title');if(/store\.getStokTbs\(\)/.test(s))throw new Error('must read stokTbs from snapshot, not getStokTbs()');console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Stock card "Edit" opens a modal with one numeric field pre-filled from `snapshot.stokTbs`.
    - Submitting a non-positive value shows "Stok harus berupa angka positif." inline; submitting a valid value calls `store.setStokTbs(numericValue)`, closes the modal, shows "Stok TBS berhasil diperbarui."
    - No `store.getStokTbs()` call exists in the render path; the card reads `snapshot.stokTbs`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>As Admin: clicking "Edit" on the stock card opens "Perbarui Stok TBS"; changing the value and saving updates the card immediately with a success toast. Navigating to KeputusanDistribusi as Manajer Distribusi shows the recommendation calculation reflecting the new stock value without a manual refresh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — add city, inline validation, duplicate-name error, capacity edit, stock editor</name>
  <action>Pause for human verification. Do not proceed until the verifier approves. Present the steps in how-to-verify and wait for the resume-signal.</action>
  <what-built>Add-city form with inline validation and duplicate-name inline error (ADMIN-02, ADMIN-06), capacity-edit for existing cities (ADMIN-03 capacity part), and the TBS stock editor wired to the real store (ADMIN-05 write side).</what-built>
  <how-to-verify>
    1. `npm run dev`, log in as Admin (`admin`/`admin123`), open "Manajemen Kota".
    2. Click "+ Tambah Kota". Leave Nama Kota blank, attempt submit → confirm "Nama kota wajib diisi." appears inline under the field (NOT a toast).
    3. Enter a capacity of 0 or a negative/non-numeric value → confirm "Kapasitas harus berupa angka positif." inline.
    4. Enter an existing name (e.g. "Medan") with a valid capacity, submit → confirm "Kota dengan nama tersebut sudah ada." appears inline under Nama Kota, the modal stays open, and there is no crash and no toast.
    5. Enter a brand-new valid name + positive capacity, submit → confirm success toast "Kota berhasil ditambahkan." and the new row appears in the table.
    6. Click Edit on an existing city, change only the capacity, save → confirm the table updates immediately with toast "Kota berhasil diperbarui."
    7. Click "Edit" on the TBS stock card, change the value, Simpan → confirm the card updates immediately with toast "Stok TBS berhasil diperbarui." Then open KeputusanDistribusi as Manajer Distribusi and confirm the recommendation reflects the new stock value without a refresh.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe what's wrong (e.g. duplicate error shows as a toast, validation missing, stock not propagating).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin form input → store mutators | User-entered nama (string) and kapasitas/stock (numeric) cross into store state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / DoS (degraded UX) | NaN kapasitas/stock propagating into computeRekomendasiDistribusi | mitigate | Page `validateForm`/`submitStock` reject non-positive/non-numeric before calling the store (D-06), plus existing store-side `Number(x) || 0` fallback — defense in depth. |
| T-02-02 | Tampering (Stored XSS) | User-entered city name rendered back into table/modals | mitigate | Render `nama` only as JSX text (React auto-escapes); never via `dangerouslySetInnerHTML` or string-built attributes. |
| T-02-03 | Information Disclosure | Duplicate-name error message | accept | Message is a benign domain string ("Kota dengan nama tersebut sudah ada."), no sensitive data; surfaced inline per D-05. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan (RESEARCH.md: "Not applicable"). No install task exists; reject any `npm install` as scope drift. |
</threat_model>

<verification>
- `npm run build` succeeds.
- Automated structural checks (both task verify commands) pass.
- Manual UAT: ADMIN-02, ADMIN-06, ADMIN-05 (write side), and ADMIN-03 capacity-edit per 01-VALIDATION.md.
- Duplicate-name error renders inline under Nama Kota, never as a toast (D-05).
</verification>

<success_criteria>
- Admin can add a city with inline validation; duplicate names show an inline error without crashing.
- Admin can edit an existing city's capacity with immediate table update.
- Admin can update the TBS stock; the change propagates to the recommendation engine without a refresh.
</success_criteria>

<output>
Create `.planning/phases/01-admin-city-stock-management/01-02-SUMMARY.md` when done.
</output>
