---
phase: 04-auth-landing-design-system-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Landing.jsx
autonomous: true
requirements: [DESIGN-01, DESIGN-04]
must_haves:
  truths:
    - "Landing imports IkonDaun from the shared component (../components/IkonDaun) and no longer defines a local duplicate IkonDaun function"
    - "Landing's hero, nav, and closing-CTA buttons render through the shared Tombol component instead of the local LandingButton"
    - "Landing's card-shaped feature surfaces (VisualCardShell and the map container) render through the shared Card component via its style-override prop"
    - "All Indonesian marketing copy (FITUR_LIST, LANGKAH_LIST, STATISTIK_LIST, FAQ_LIST, TESTIMONIAL_CHIPS, hero headline/subtext, footer) is preserved verbatim"
    - "src/components/Tombol.jsx, src/components/Card.jsx, src/components/IkonDaun.jsx are byte-unchanged"
    - "Dashboard.jsx, ManajemenKota.jsx, and one other Tombol/Card-consuming page (e.g. InputData.jsx) render with no visual or layout regression after the Landing migration (DESIGN-04)"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "Landing marketing page rebuilt on shared Tombol/Card/IkonDaun"
      contains: "import IkonDaun from \"../components/IkonDaun\""
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "src/components/IkonDaun.jsx"
      via: "import IkonDaun from '../components/IkonDaun' replacing local duplicate"
      pattern: "import IkonDaun from \"../components/IkonDaun\""
    - from: "src/pages/Landing.jsx"
      to: "src/components/Tombol.jsx"
      via: "import Tombol; hero/nav/CTA buttons rendered as <Tombol .../>"
      pattern: "import Tombol from \"../components/Tombol\""
    - from: "src/pages/Landing.jsx"
      to: "src/components/Card.jsx"
      via: "import Card; VisualCardShell + map container rendered as <Card style={{...}}>"
      pattern: "import Card from \"../components/Card\""
---

<objective>
Rebuild `src/pages/Landing.jsx` on the shared component library — delete the duplicated local `IkonDaun`, migrate all `LandingButton` call sites to the shared `Tombol`, and convert the bespoke card-shaped surfaces (`VisualCardShell`, map container) to the shared `Card` via its `style`-override prop — satisfying DESIGN-01 while preserving every section, all layout intent, and all Indonesian copy exactly.

Purpose: Closes the marketing-page half of design-system unification. Landing currently hand-rolls its own button system (`landing-btn-*`), shadows the shared `IkonDaun`, and builds bespoke card surfaces — this is the largest single source of the "feels like a different product" inconsistency the phase targets.
Output: `src/pages/Landing.jsx` rendering through `Tombol`, `Card`, and the shared `IkonDaun`, with `LandingButton`/`SIZE_STYLES`/local `IkonDaun` removed and the now-unused `useRipple`/`RippleSpans` cleaned up, and zero edits to any shared component file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auth-landing-design-system-unification/04-PATTERNS.md
@.planning/phases/04-auth-landing-design-system-unification/04-UI-SPEC.md
@src/components/Tombol.jsx
@src/components/Card.jsx
@src/components/IkonDaun.jsx
</context>

## Phase Goal

**As a** visitor landing on the Switera marketing page, **I want to** see the same buttons, surfaces, and brand mark used throughout the app, **so that** the product feels like one coherent, finished system rather than a separately-styled brochure.

## Artifacts this phase produces

This plan creates no new exported symbols. It is a refactor-in-place that:
- Adds `import Tombol from "../components/Tombol";`, `import Card from "../components/Card";`, and `import IkonDaun from "../components/IkonDaun";` to `src/pages/Landing.jsx`
- Deletes the local `IkonDaun` function (currently lines 133-157)
- Deletes the local `LandingButton` function and `SIZE_STYLES` map (currently lines 398-419)
- Deletes the now-unused local `RippleSpans` function (currently line 277) and the `useRipple` import (line 2) if no other reference remains
- Converts `VisualCardShell` (line 612) and the map-section container (line 1463) to use `<Card style={{...}}>`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: De-duplicate IkonDaun — delete local copy, import shared</name>
  <files>src/pages/Landing.jsx</files>
  <read_first>
    - src/pages/Landing.jsx (lines 1-160 for the import block + local IkonDaun; lines 1019 and 1605 for the two IkonDaun call sites)
    - src/components/IkonDaun.jsx (the shared component — confirm byte-identical default props size=22, color="var(--color-primary)")
    - .planning/phases/04-auth-landing-design-system-unification/04-PATTERNS.md (Risk 3 / IkonDaun de-duplication section)
  </read_first>
  <action>
    Add `import IkonDaun from "../components/IkonDaun";` to the import block at the top of Landing.jsx (near the existing imports at lines 1-7).

    Delete the entire local `function IkonDaun({ size = 22, color = "var(--color-primary)" }) { ... }` definition (currently lines 133-157). The shared component is byte-identical (same viewBox `0 0 48 48`, same two `path` d-values, same default props), so the two existing call sites — `<IkonDaun size={18} />` and `<IkonDaun size={16} />` (currently lines 1019 and 1605) — require NO change and will render identically against the shared import.

    Do not touch `IkonArrowRight` or any other local icon function — only the duplicated `IkonDaun` is removed. Do not modify the call sites.
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && node -e "const s=require('fs').readFileSync('src/pages/Landing.jsx','utf8'); const ok = s.includes('import IkonDaun from \"../components/IkonDaun\"') && !(/function IkonDaun\s*\(/.test(s)) && s.includes('<IkonDaun size={18}') && s.includes('<IkonDaun size={16}'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Landing.jsx` contains the literal `import IkonDaun from "../components/IkonDaun";`
    - No `function IkonDaun(` declaration remains in `src/pages/Landing.jsx`
    - Both call sites `<IkonDaun size={18} />` and `<IkonDaun size={16} />` are still present and unchanged
    - `git diff --stat src/components/IkonDaun.jsx` produces no output
  </acceptance_criteria>
  <done>Local IkonDaun duplicate deleted; shared IkonDaun imported; both call sites render unchanged against the shared component.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate LandingButton call sites to Tombol and remove dead button code</name>
  <files>src/pages/Landing.jsx</files>
  <read_first>
    - src/pages/Landing.jsx (lines 1-10 import block; line 277 local RippleSpans; lines 398-419 SIZE_STYLES + LandingButton; the 5 call sites at lines 1077, 1078, 1178-1184, 1185-1192, 1580-1586)
    - src/components/Tombol.jsx (6 props: label, variant, onClick, type, disabled, style — NO size and NO iconRight prop; ripple internal)
    - .planning/phases/04-auth-landing-design-system-unification/04-PATTERNS.md (section "src/pages/Landing.jsx" item 1, and Risk 1 locked decision: use variant="primer" for primary CTAs, variant="sekunder" for ghost/Masuk nav button)
    - .planning/phases/04-auth-landing-design-system-unification/04-UI-SPEC.md (Risk 1 — green primer fill replaces the white-inverted treatment is the intended, on-brand change)
  </read_first>
  <action>
    Add `import Tombol from "../components/Tombol";` to the import block.

    Replace all 5 `LandingButton` call sites with `Tombol`, per UI-SPEC Risk 1 (locked decision — Option 1). `Tombol` has no `size` and no `iconRight` prop, so express sizing via the `style` prop and fold any trailing icon into the `label` node:
    - Nav "Masuk" (line 1077, currently `tone="ghost" size="sm"`): becomes `<Tombol variant="sekunder" label="Masuk" onClick={openLogin} style={{ padding: "7px 16px", fontSize: "var(--text-xs)" }} />` (carry the old `SIZE_STYLES.sm` padding/fontSize into style).
    - Nav "Daftar" (line 1078, currently `tone="primer" size="sm"`): becomes `<Tombol variant="primer" label="Daftar" onClick={openRegister} style={{ padding: "7px 16px", fontSize: "var(--text-xs)" }} />`.
    - Hero "Mulai Sekarang" (lines 1178-1184, `tone="primer" size="md"` with style override `padding: "10px 22px", fontSize: "14px", borderRadius: "8px"`): becomes `<Tombol variant="primer" label="Mulai Sekarang" onClick={openRegister} style={{ padding: "10px 22px", fontSize: "14px", borderRadius: "8px" }} />`.
    - Hero "Lihat Demo" (lines 1185-1192, `tone="ghost" size="md"`, `iconRight={<IkonArrowRight size={13} />}`, style `padding: "10px 16px", fontSize: "14px", fontWeight: 400`): becomes `<Tombol variant="sekunder" onClick={() => scrollToId("fitur")} style={{ padding: "10px 16px", fontSize: "14px", fontWeight: 400 }} label={<span style={{ display: "inline-flex", alignItems: "center", gap: "var(--space-1)" }}>Lihat Demo <IkonArrowRight size={13} /></span>} />` — fold the trailing arrow icon into the `label` node since `Tombol` has no `iconRight` prop.
    - Closing CTA "Daftar Sekarang" (lines 1580-1586, `tone="primer" size="md"`, style `padding: "12px 28px", fontSize: "14px"`): becomes `<Tombol variant="primer" label="Daftar Sekarang" onClick={openRegister} style={{ padding: "12px 28px", fontSize: "14px" }} />`.

    Preserve every visible button label string exactly ("Masuk", "Daftar", "Mulai Sekarang", "Lihat Demo", "Daftar Sekarang") and every `onClick` handler (`openLogin`, `openRegister`, `scrollToId("fitur")`).

    After all 5 call sites are migrated, delete the now-dead local code: the `SIZE_STYLES` const and `LandingButton` function (lines 398-419), the local `RippleSpans` function (line 277), and the `useRipple` import (line 2) — but FIRST grep-confirm none of these three are referenced elsewhere in the file after migration. Only remove a symbol if it has zero remaining references. (Expected: after migration, `LandingButton`, `SIZE_STYLES`, `RippleSpans`, and `useRipple` each have zero references and are all safe to delete.)
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && node -e "const s=require('fs').readFileSync('src/pages/Landing.jsx','utf8'); const ok = s.includes('import Tombol from \"../components/Tombol\"') && !s.includes('LandingButton') && !s.includes('SIZE_STYLES') && !(/function RippleSpans/.test(s)) && !(/from \"\.\.\/hooks\/useRipple\"/.test(s)) && ['Mulai Sekarang','Lihat Demo','Daftar Sekarang','\"Masuk\"','\"Daftar\"'].every(t=>s.includes(t)); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Landing.jsx` contains the literal `import Tombol from "../components/Tombol";`
    - The identifiers `LandingButton` and `SIZE_STYLES` no longer appear anywhere in `src/pages/Landing.jsx`
    - No `function RippleSpans` declaration and no `import ... from "../hooks/useRipple"` remain (both became dead code after migration)
    - All 5 button labels still appear: "Mulai Sekarang", "Lihat Demo", "Daftar Sekarang", "Masuk", "Daftar"
    - Each migrated nav/CTA button uses `variant="primer"` (Daftar, Mulai Sekarang, Daftar Sekarang) or `variant="sekunder"` (Masuk, Lihat Demo)
    - The "Lihat Demo" button's `IkonArrowRight` icon is folded into its `label` node (no `iconRight` prop on Tombol)
    - `git diff --stat src/components/Tombol.jsx` produces no output
  </acceptance_criteria>
  <done>All 5 LandingButton call sites render via Tombol with correct variants and preserved labels/handlers; LandingButton, SIZE_STYLES, local RippleSpans, and useRipple import removed as dead code; Tombol.jsx untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Adopt shared Card for VisualCardShell and the map container</name>
  <files>src/pages/Landing.jsx</files>
  <read_first>
    - src/pages/Landing.jsx (lines 612-627 VisualCardShell outer div; lines 1462-1473 map-section container div)
    - src/components/Card.jsx (Card merges a passed `style` object on top of its defaults: backgroundColor var(--color-surface), borderRadius var(--radius-lg), padding var(--space-6), fadeInUp animation; accepts arbitrary props via {...props})
    - .planning/phases/04-auth-landing-design-system-unification/04-PATTERNS.md (section "src/pages/Landing.jsx" item 2 + Risk 2 resolution: convert bespoke card divs to <Card style={{...the same overrides...}}>)
    - .planning/phases/04-auth-landing-design-system-unification/04-UI-SPEC.md (Risk 2 — additive style override only; do NOT apply Card to hero glow or MockupDashboard chrome)
  </read_first>
  <action>
    Import the shared Card: add `import Card from "../components/Card";` to the import block.

    Convert the `VisualCardShell` outer wrapper `<div>` (currently lines 614-627, with backgroundColor var(--color-surface), border 1px solid var(--color-border-mid), borderRadius 14px, padding 28px, boxShadow "0 20px 60px rgba(0,0,0,0.4)", aspectRatio "4 / 3", position relative, overflow hidden, display flex, flexDirection column) into `<Card style={{ border: "1px solid var(--color-border-mid)", borderRadius: "14px", padding: "28px", boxShadow: "0 20px 60px rgba(0,0,0,0.4)", aspectRatio: "4 / 3", position: "relative", overflow: "hidden", display: "flex", flexDirection: "column" }}>`. Card already supplies `backgroundColor: var(--color-surface)` as a default, so the bespoke values that differ from Card's defaults (custom border, 14px radius, 28px padding, custom boxShadow, aspectRatio, etc.) are passed via the `style` override — this preserves the exact current appearance. Keep all of `VisualCardShell`'s inner children (the icon chip div and `{children}`) unchanged. Change only the closing `</div>` of the outer wrapper to `</Card>`.

    Convert the map-section container `<div>` (currently lines 1463-1470, with border 1px solid var(--color-border-mid), borderRadius 14px, overflow hidden, boxShadow "0 20px 60px rgba(0,0,0,0.4)") into `<Card style={{ border: "1px solid var(--color-border-mid)", borderRadius: "14px", overflow: "hidden", boxShadow: "0 20px 60px rgba(0,0,0,0.4)", padding: 0 }}>` wrapping the `<PetaGeografis ... />` child. Note: pass `padding: 0` in the override because this container is a flush map frame (the current div has no padding) and Card defaults to `var(--space-6)` padding — without `padding: 0` the map would gain unwanted inset. Keep the `<PetaGeografis ranking={rankingDemo} daftarKota={daftarKotaDemo} />` child unchanged; change only the wrapper's closing `</div>` to `</Card>`.

    Do NOT apply Card to: the hero glow gradient, the `MockupDashboard` chrome, the FAQ accordion container, or the testimonial-chip spans — UI-SPEC explicitly excludes the decorative/non-card-shaped surfaces, and the FAQ/testimonial conversions are discretionary and out of scope for this task to avoid layout churn on pill/accordion elements that are not semantically card surfaces.
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && node -e "const s=require('fs').readFileSync('src/pages/Landing.jsx','utf8'); const cardCount=(s.match(/<Card[ >]/g)||[]).length; const ok = s.includes('import Card from \"../components/Card\"') && cardCount>=2 && s.includes('<PetaGeografis') && s.includes('aspectRatio: \"4 / 3\"'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/Landing.jsx` contains the literal `import Card from "../components/Card";`
    - At least two `<Card ...>` elements are rendered (VisualCardShell wrapper + map container)
    - The map-section `<Card>` wraps `<PetaGeografis ranking={rankingDemo} daftarKota={daftarKotaDemo} />` and passes `padding: 0` in its style override
    - The `VisualCardShell` `<Card>` preserves `aspectRatio: "4 / 3"`, `padding: "28px"`, and `boxShadow: "0 20px 60px rgba(0,0,0,0.4)"` via the style prop
    - No `<Card>` is applied to the hero glow, MockupDashboard, FAQ accordion, or testimonial chips
    - `git diff --stat src/components/Card.jsx` produces no output
  </acceptance_criteria>
  <done>VisualCardShell and the map container render via shared Card with style-override preserving exact appearance; decorative/non-card surfaces untouched; Card.jsx unchanged.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 4: Visually spot-check other Tombol/Card-consuming pages for regression (DESIGN-04)</name>
  <files>src/pages/Dashboard.jsx, src/pages/ManajemenKota.jsx, src/pages/InputData.jsx</files>
  <read_first>
    - .planning/phases/04-auth-landing-design-system-unification/04-UI-SPEC.md (Shared-Component Risk Register, line 209: "Mandatory plan-phase instruction" — this task is that instruction)
  </read_first>
  <action>
    After Tasks 1-3 are complete and `Tombol.jsx`/`Card.jsx`/`IkonDaun.jsx` are confirmed byte-unchanged, run the dev server and visually load Dashboard.jsx, ManajemenKota.jsx, and one other Tombol/Card-consuming page (e.g. InputData.jsx or KeputusanDistribusi.jsx). Confirm buttons and card surfaces on these pages render identically to before this plan — same colors, padding, borders, shadows, and layout. This is the DESIGN-04 acceptance check: since Landing's Task 2/3 only ever pass a `style` override at the Landing call site and never edit the shared component source files, other consumers should be visually unaffected, but this must be confirmed by inspection, not inferred solely from the byte-unchanged diff check.
  </action>
  <verify>
    <manual>Load /dashboard, /manajemen-kota (or equivalent route), and one other page in a browser; visually compare button and card rendering against pre-change appearance (or against a fresh `git stash` baseline if needed). No visual or layout regression should be observed.</manual>
  </verify>
  <acceptance_criteria>
    - Dashboard.jsx's buttons/cards render with no visual or layout regression
    - ManajemenKota.jsx's buttons/cards render with no visual or layout regression
    - At least one additional Tombol/Card-consuming page renders with no visual or layout regression
  </acceptance_criteria>
  <done>Visual spot-check of Dashboard.jsx, ManajemenKota.jsx, and one other shared-component-consuming page confirms zero regression, closing DESIGN-04 for this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor→Landing page | Landing renders seeded demo data (store.getPermintaan / store.getDaftarKota) read-only; no untrusted write input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Landing component refactor (button/card/icon swap) | mitigate | Changes are rendering-surface-only; the read-only `aggregatePermintaanRanking`/`store.get*` demo-data flow is untouched — acceptance criteria assert `<PetaGeografis>` data props and demo wiring are preserved |
| T-04-04 | Denial of Service | Dead-code removal (useRipple/RippleSpans/LandingButton) | mitigate | Removal is gated on grep-confirming zero remaining references before deletion, preventing a broken-import runtime crash; verified by `npm run build` succeeding |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (consumes existing first-party components only); no legitimacy checkpoint required |
</threat_model>

<verification>
- `npx vite build` completes without error, confirming no dangling references after `LandingButton`/`SIZE_STYLES`/`RippleSpans`/`useRipple`/local-`IkonDaun` removal.
- `git diff --stat src/components/Tombol.jsx src/components/Card.jsx src/components/IkonDaun.jsx` produces no output (shared components byte-unchanged).
- Manual: load Landing → confirm all 9 sections (header/nav, hero, stats strip, features, Cara Kerja, map, FAQ, closing CTA, footer) render in order with all Indonesian copy intact; buttons are now green `primer`/bordered `sekunder` (the intended on-brand change per UI-SPEC Risk 1); the feature visuals and map frame still show the card surface treatment.
- Manual (DESIGN-04): visually spot-check Dashboard.jsx, ManajemenKota.jsx, and one other Tombol/Card-consuming page — confirm no visual or layout regression (Task 4).
</verification>

<success_criteria>
- DESIGN-01: Landing renders using `Tombol`, `Card`, `IkonDaun`, and design tokens, with the same content and layout intent (all 9 sections, all copy) as before.
- DESIGN-04: Dashboard.jsx, ManajemenKota.jsx, and one other Tombol/Card-consuming page show no visual or layout regression after this plan.
- Local `IkonDaun` duplicate, `LandingButton`, `SIZE_STYLES`, local `RippleSpans`, and the `useRipple` import are removed.
- Zero edits to `src/components/Tombol.jsx`, `src/components/Card.jsx`, `src/components/IkonDaun.jsx`.
</success_criteria>

<output>
Create `.planning/phases/04-auth-landing-design-system-unification/04-02-SUMMARY.md` when done.
</output>
