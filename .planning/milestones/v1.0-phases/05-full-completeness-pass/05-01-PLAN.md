---
phase: 05-full-completeness-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.jsx
  - src/pages/KeputusanDistribusi.jsx
  - src/pages/Register.jsx
  - src/store.js
autonomous: true
requirements: [AUDIT-01]
---

<objective>
Run a full completeness audit across all 12 pages in `src/pages/` against the 9-dimension checklist (complete UI, CRUD via store, role-based data, navigation, inline validation, empty states, loading states, design-system consistency, no-reload data flow), then fix every concrete gap found.

Purpose: AUDIT-01 requires re-verifying every page and closing any gap beyond what Phases 1-4 already fixed, not just logging it.
Output: 4 concrete inconsistency/validation gaps fixed across 4 files; 2 additional findings (Login.jsx's non-functional "Lupa Password?" link and "Ingat saya" checkbox) explicitly reviewed with the user and dispositioned as no-action (the former defers intentionally to a planned future backend milestone; the latter accepted as a harmless placeholder).
</objective>

## Audit Method

A read-only audit agent reviewed all 12 pages against the checklist (priority depth on the 5 pages never the explicit subject of a prior phase: `AnalisisRanking.jsx`, `Dashboard.jsx`, `KeputusanDistribusi.jsx`, `ManajemenData.jsx`, `RiwayatAktivitas.jsx`; lighter regression pass on the other 7). Findings were manually verified against the actual code before any fix was applied.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Require armada/ETA before saving "Dalam Pengiriman" from Dashboard's quick-action modal</name>
  <files>src/pages/Dashboard.jsx</files>
  <action>
    `DashboardLogistik`'s status-update modal let Tim Logistik save "Dalam Pengiriman" with blank armada/ETA — the same action `StatusDistribusi.jsx` already validates. Ported `validateModalForm`/`modalErrors`/`errorStyle` and the danger-border-on-error pattern verbatim from `StatusDistribusi.jsx:77-113,246-298` into `DashboardLogistik` (`Dashboard.jsx:1129-1490`).
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && npx vite build</automated>
  </verify>
  <done>Dashboard's quick-action modal now blocks saving "Dalam Pengiriman" with blank armada/ETA, matching StatusDistribusi.jsx's existing behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Block duplicate active decisions in KeputusanDistribusi</name>
  <files>src/pages/KeputusanDistribusi.jsx</files>
  <action>
    `saveKeputusan` had no guard against creating a second active decision for a city that already has one, while `Dashboard.jsx`'s `handleTetapkanDistribusi` already checks for this (`Dashboard.jsx:843-855`). Ported the same `existingDecision` check into `saveKeputusan` (`KeputusanDistribusi.jsx:49`), surfaced via the file's existing `showToast` pattern.
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && npx vite build</automated>
  </verify>
  <done>KeputusanDistribusi now blocks a second active decision for the same city, matching Dashboard.jsx's existing guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Clear only the touched field's error in Register</name>
  <files>src/pages/Register.jsx</files>
  <action>
    All 4 field `onChange` handlers called `setErrors({})`, wiping every other field's visible error on any single keystroke. Switched to per-field clearing (`setErrors(prev => ({...prev, field: undefined}))`), matching `Login.jsx`'s existing pattern.
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && npx vite build</automated>
  </verify>
  <done>Register's field errors clear independently per field, no longer hiding other fields' unresolved errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Guard updateKota against renaming to a duplicate name</name>
  <files>src/store.js</files>
  <action>
    `tambahKota` already rejects a duplicate city name; `updateKota` had no equivalent check. Added the same `daftarKota.some(...)` guard (excluding the city's own current name) before applying the rename, throwing the same `"Kota dengan nama tersebut sudah ada."` error — already caught and surfaced by `ManajemenKota.jsx:163-181`'s existing try/catch, so no UI change was needed.
  </action>
  <verify>
    <automated>cd "C:/Users/acer/Switera" && npx vite build</automated>
  </verify>
  <done>Renaming a city to a name already in use is rejected with the same inline error tambahKota already shows for new cities.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | All 4 fixes | mitigate | Each fix ports an already-reviewed validation/guard pattern from an existing file to a new location — no new validation logic was invented, reducing risk of introducing a new flaw |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `npx vite build` succeeds with zero errors after each task.
- Manual trace: each fix's guard condition matches its reference implementation's condition exactly (confirmed by direct file comparison before editing).
</verification>

<success_criteria>
- AUDIT-01: all 12 pages re-verified against the 9-dimension checklist; 4 concrete gaps found and fixed; 2 findings explicitly dispositioned with the user as no-action (not silently dropped).
</success_criteria>

<output>
Create `.planning/phases/05-full-completeness-pass/05-01-SUMMARY.md` when done.
</output>
