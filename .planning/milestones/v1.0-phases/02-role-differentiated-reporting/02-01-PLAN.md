---
phase: 02-role-differentiated-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Laporan.jsx
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03]

must_haves:
  truths:
    - "When roleAktif is 'Manajer Distribusi', Laporan shows the Riwayat Keputusan table sourced from riwayatKeputusan and the Tren Permintaan line chart sourced from permintaan (unchanged from current behavior)"
    - "When roleAktif is 'Tim Logistik', Laporan shows a Distribusi Aktif table sourced from keputusan (with an Armada/ETA column) and a Status Pengiriman doughnut chart counting keputusan by status"
    - "Switching roleAktif (e.g. via login as a different role) changes which table/chart data source Laporan reads — not just the page heading text"
    - "Ekspor CSV on Tim Logistik's view downloads armada+eta+status columns under a laporan-status-{periode} filename; Manajer Distribusi's CSV export is unchanged (laporan-distribusi-{periode}, no armada/eta columns)"
    - "Each role's empty states show role-specific copy when its table or chart has no data for the selected periode"
  artifacts:
    - path: "src/pages/Laporan.jsx"
      provides: "Role-conditional Laporan page: Manajer Distribusi keputusan-history view vs Tim Logistik status-delivery view, both filtered by the existing periode control"
      contains: "GrafikStatusPengiriman"
  key_links:
    - from: "src/pages/Laporan.jsx"
      to: "src/store.js"
      via: "snapshot.roleAktif read from store.getState()/store.subscribe(), branches which of snapshot.riwayatKeputusan vs snapshot.keputusan is used"
      pattern: "roleAktif === \"Tim Logistik\""
    - from: "src/pages/Laporan.jsx"
      to: "src/utils/csv.js"
      via: "handleExportCsv calls downloadCsv() with role-conditional filename and row shape"
      pattern: "downloadCsv\\(`laporan-(distribusi|status)-"
    - from: "src/pages/Laporan.jsx (GrafikStatusPengiriman)"
      to: "chart.js/auto"
      via: "dynamic import(\"chart.js/auto\") inside useEffect, same lifecycle pattern as GrafikTrenPermintaan"
      pattern: "import\\(\"chart.js/auto\"\\)"
---

<objective>
Make the Laporan page genuinely role-differentiated: Manajer Distribusi keeps the current keputusan-history view; Tim Logistik gets a new status/delivery-focused view (active distributions with armada/ETA, plus a status-distribution chart) reading from different store collections, not just a relabeled heading.

Purpose: Closes REPORT-01/02/03 — currently `roleAktif` is detected in Laporan.jsx (line 211-213) but never used to branch what data is shown; both roles see the identical Manajer-centric report.
Output: `src/pages/Laporan.jsx` rebuilt with a role-conditional data layer, a new `GrafikStatusPengiriman` chart component, and role-conditional CSV export — same page shell, same shared components, same design tokens (per UI-SPEC.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-role-differentiated-reporting/02-UI-SPEC.md
@.planning/phases/02-role-differentiated-reporting/02-PATTERNS.md
@src/pages/Laporan.jsx
@src/pages/StatusDistribusi.jsx
@src/utils/distribusi.js
@src/utils/chartDefaults.js
@src/utils/csv.js
@src/utils/format.js
@src/components/Tabel.jsx
@src/components/Badge.jsx
@src/components/EmptyState.jsx
@src/store.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add role-conditional data layer and Tim Logistik table to Laporan.jsx</name>
  <files>src/pages/Laporan.jsx</files>
  <read_first>
    src/pages/Laporan.jsx (full file — current single-role implementation, lines 1-357)
    src/pages/StatusDistribusi.jsx (lines 38-64 — keputusanAktif useMemo sort pattern and rows.map armada/eta concatenation pattern; lines 15-20 — statusOptions/statusLabels constants)
    src/utils/distribusi.js (lines 1, 78-103 — parseDate, getPeriodRange, isDateInRange signatures, already imported in Laporan.jsx)
    src/store.js (lines 409-413 — getKeputusan/getRiwayatKeputusan return clone(state.keputusan) / clone(state.riwayatKeputusan); confirms snapshot.keputusan and snapshot.riwayatKeputusan are the two distinct collections)
  </read_first>
  <action>
    In src/pages/Laporan.jsx, after the existing `roleAktif` derivation (currently lines 211-213, keep unchanged), add a boolean `const isTimLogistik = roleAktif === "Tim Logistik";`.

    Add a `statusLabels` module-level constant identical in shape to StatusDistribusi.jsx lines 16-20 (`{ menunggu: "Menunggu", "dalam-pengiriman": "Dalam Pengiriman", selesai: "Selesai" }`) — place it next to the existing `roleOptions`/`periodeOptions` constants near the top of the file.

    Add a new `filteredKeputusan` useMemo, modeled on the existing `filteredRiwayat` useMemo (lines 217-226): source `snapshot.keputusan` (not `snapshot.riwayatKeputusan`), filter by `isDateInRange(item.tanggal_keputusan, range)`, sort descending by `parseDate(item.tanggal_keputusan)`. Dependency array: `[range, snapshot.keputusan]`.

    Add a `statusCounts` useMemo that reduces `filteredKeputusan` into `{ menunggu: 0, "dalam-pengiriman": 0, selesai: 0 }` counts keyed by `item.status` (same counting pattern as StatusDistribusi.jsx's `ringkasanStatus`, lines 47-55), dependency `[filteredKeputusan]`.

    Add a `tableRowsTimLogistik` derived array (plain map, not useMemo, matching the existing `tableRows` style at lines 236-243) built from `filteredKeputusan`: `{ id: item.id, tanggal: formatDate(item.tanggal_keputusan), kotaTujuan: item.kota_tujuan, volume: formatTonase(item.volume_tbs), armada: item.armada ? \`${item.armada}${item.eta ? \` · ETA ${formatDate(item.eta)}\` : ""}\` : "-", status: <Badge status={item.status} /> }` — this mirrors StatusDistribusi.jsx lines 57-64's armada/eta concatenation exactly.

    Rename the existing `tableRows` variable to `tableRowsManajer` (update its one usage at the `<Tabel data={...}>` call) to keep the two role table-row arrays visually distinct and avoid ambiguity. Keep `noData` (lines 269-270) but compute it conditionally: when `isTimLogistik`, base it on `filteredKeputusan.length === 0 && statusCounts.menunggu + statusCounts["dalam-pengiriman"] + statusCounts.selesai === 0`; otherwise keep current logic (`filteredRiwayat.length === 0 && chartConfig.labels.length === 0`).

    Do not touch the JSX render section yet — that is Task 2.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isTimLogistik" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "filteredKeputusan" src/pages/Laporan.jsx` returns at least 2 (declaration + usage)
    - `grep -c "statusCounts" src/pages/Laporan.jsx` returns at least 2
    - `grep -c "tableRowsTimLogistik" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "tableRowsManajer" src/pages/Laporan.jsx` returns at least 2 (declaration + usage in Tabel)
    - `grep -c "statusLabels" src/pages/Laporan.jsx` returns at least 1
    - `npm run build` exits 0 (Vite build succeeds, confirms no syntax errors)
  </acceptance_criteria>
  <done>Laporan.jsx has a complete role-conditional data layer (filteredKeputusan, statusCounts, tableRowsTimLogistik, tableRowsManajer) without any render/JSX changes yet; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add GrafikStatusPengiriman chart, branch the JSX render and CSV export by role</name>
  <files>src/pages/Laporan.jsx</files>
  <read_first>
    src/pages/Laporan.jsx (the GrafikTrenPermintaan function, lines 77-197 — exact chart lifecycle pattern to replicate for the new chart component; the render return block, lines 284-353)
    src/utils/chartDefaults.js (full file — CHART_PALETTE, withOpacity, chartLegendDefaults, chartTooltipDefaults exports used by doughnut config)
    .planning/phases/02-role-differentiated-reporting/02-UI-SPEC.md (Content Visibility & Data Contract section — exact copy strings for both roles; CSV Export Contract section — exact column lists and filenames)
  </read_first>
  <action>
    Add a new component `GrafikStatusPengiriman({ counts })` directly below `GrafikTrenPermintaan` in src/pages/Laporan.jsx, following the identical lifecycle structure (useRef canvasRef, useState chartError/isChartReady, useEffect with dynamic `import("chart.js/auto")`, isActive flag, cleanup destroying chartInstance). Guard condition: return early (undefined) when `!canvasRef.current` or when the total of `counts.menunggu + counts["dalam-pengiriman"] + counts.selesai === 0`. Chart config: `type: "doughnut"`, `data.labels: [statusLabels.menunggu, statusLabels["dalam-pengiriman"], statusLabels.selesai]`, single dataset with `data: [counts.menunggu, counts["dalam-pengiriman"], counts.selesai]`, `backgroundColor` and `borderColor` from `CHART_PALETTE[0..2]` via `withOpacity(color, 0.7)` for backgroundColor and the raw color for borderColor, `borderWidth: 2`. Options: `responsive: true, maintainAspectRatio: false, plugins: { legend: { position: "bottom", ...chartLegendDefaults }, tooltip: { ...chartTooltipDefaults } }, cutout: "60%"`. Wrap the canvas in the same `<Card style={{ minHeight: "420px" }}>` + `<SectionHeader>Status Pengiriman</SectionHeader>` + skeleton/error pattern as GrafikTrenPermintaan (lines 176-196), substituting the canvas `aria-label` to `"Grafik status pengiriman"` and the error message to `"Grafik tidak dapat dimuat karena Chart.js belum tersedia."` (same string, reused).

    Update `handleExportCsv` (currently lines 272-282): branch on `isTimLogistik`. When true, map `filteredKeputusan` to `{ tanggal: item.tanggal_keputusan, kota_tujuan: item.kota_tujuan, volume_tbs: item.volume_tbs, armada: item.armada ?? "", eta: item.eta ?? "", status: item.status }` and call `downloadCsv(\`laporan-status-${periode}.csv\`, rows)`. When false, keep the existing `filteredRiwayat`-based mapping and `laporan-distribusi-${periode}.csv` filename exactly as-is.

    Update the `<PageHeader>` props (currently lines 286-300): `judul` becomes `isTimLogistik ? "Laporan Status Distribusi" : "Laporan Distribusi"`; `deskripsi` becomes `isTimLogistik ? "Pantau progres distribusi aktif dan riwayat pengiriman per periode." : "Riwayat keputusan bersifat permanen dan tetap tersedia untuk audit periode sebelumnya."`. The `aksi` block (PeriodePills + Tombol Ekspor CSV) stays structurally identical; only update the `disabled` prop on the Ekspor CSV Tombol to `isTimLogistik ? filteredKeputusan.length === 0 : filteredRiwayat.length === 0`.

    Replace the body of the data-sections `<div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>` (currently lines 301-351): keep the existing `{noData ? <EmptyState pesan="Tidak ada data pada periode yang dipilih." /> : (...)}` outer shape, but inside the non-empty branch, render two parallel JSX blocks gated by `isTimLogistik`:
    - Tim Logistik primary section: `<Card><SectionHeader>Distribusi Aktif</SectionHeader><p>Seluruh distribusi dengan detail armada dan status pengiriman untuk audit periode sebelumnya.</p><Tabel kolom={[{key:"tanggal",label:"Tanggal"},{key:"kotaTujuan",label:"Kota Tujuan"},{key:"volume",label:"Volume TBS",numeric:true},{key:"armada",label:"Armada / ETA"},{key:"status",label:"Status"}]} data={tableRowsTimLogistik} /></Card>` when `filteredKeputusan.length > 0`, else `<EmptyState pesan="Belum ada distribusi aktif pada periode yang dipilih." />`.
    - Tim Logistik secondary section: `<GrafikStatusPengiriman counts={statusCounts} />` when `statusCounts.menunggu + statusCounts["dalam-pengiriman"] + statusCounts.selesai > 0`, else `<EmptyState pesan="Belum ada data status distribusi pada periode yang dipilih." />`.
    - Manajer Distribusi branch: keep the existing two blocks unchanged (Riwayat Keputusan Card with `tableRowsManajer` and the existing `GrafikTrenPermintaan` conditional), just gated under the `!isTimLogistik` branch of the same conditional.

    Use a single ternary or if/else JSX fragment keyed on `isTimLogistik` wrapping both sections per role — do not duplicate the outer `noData` / fragment wrapper, only the two inner content blocks differ per role.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GrafikStatusPengiriman" src/pages/Laporan.jsx` returns at least 2 (function definition + JSX usage)
    - `grep -c "doughnut" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "laporan-status-" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "laporan-distribusi-" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "Distribusi Aktif" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "Status Pengiriman" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "Laporan Status Distribusi" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "Belum ada distribusi aktif pada periode yang dipilih" src/pages/Laporan.jsx` returns at least 1
    - `grep -c "Belum ada data status distribusi pada periode yang dipilih" src/pages/Laporan.jsx` returns at least 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Laporan.jsx renders a fully role-conditional page: Tim Logistik shows Distribusi Aktif table + Status Pengiriman doughnut chart from snapshot.keputusan; Manajer Distribusi shows the original Riwayat Keputusan table + Tren Permintaan line chart from snapshot.riwayatKeputusan/permintaan; CSV export and page heading also differ per role; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| store→Laporan render | `snapshot.roleAktif`, `snapshot.keputusan`, `snapshot.riwayatKeputusan` are client-controlled localStorage-backed state; Laporan.jsx trusts and renders them directly with no server validation (consistent with existing app-wide model) |
| user→CSV export | `downloadCsv()` writes city/status/armada string fields directly into CSV cells without sanitization (pre-existing utils/csv.js behavior, not modified by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-01 | Tampering | Laporan.jsx roleAktif branch | accept | `roleAktif` is validated against `roleOptions` whitelist with a safe default to "Manajer Distribusi" (existing pattern, lines 211-213, unchanged) — a tampered/invalid localStorage value cannot produce an undefined branch, only falls back to the Manajer view |
| T-02-02 | Information Disclosure | GrafikStatusPengiriman / Distribusi Aktif table | accept | Same-origin client-only app; both roles already have store-level read access to all collections via `store.getState()` — this phase only changes which fields are *displayed*, not what is *readable*, so no new disclosure surface is introduced |
| T-02-03 | Tampering | CSV injection via city/armada free-text fields exported to CSV | accept | Pre-existing in `utils/csv.js`/current Laporan CSV export; tracked separately as SEC-01 in REQUIREMENTS.md v2 (deferred), out of scope for this phase per CONTEXT.md boundary (data-source branching only, no new input surfaces added) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (uses existing chart.js dependency already in package.json) — no legitimacy checkpoint required |

</threat_model>

<verification>
1. Run `npm run build` — must exit 0 with no errors.
2. Manual role-switch check (covered by must_haves key_links): with `roleAktif` set to "Manajer Distribusi" in localStorage/login, Laporan shows "Laporan Distribusi" heading, Riwayat Keputusan table, Tren Permintaan line chart.
3. With `roleAktif` set to "Tim Logistik", Laporan shows "Laporan Status Distribusi" heading, Distribusi Aktif table (with Armada/ETA column), Status Pengiriman doughnut chart.
4. Ekspor CSV button on Tim Logistik view downloads a file named `laporan-status-{periode}.csv` containing armada/eta/status columns; on Manajer Distribusi it downloads `laporan-distribusi-{periode}.csv` with the original columns (diputuskan_oleh, no armada/eta).
5. `grep -c "GrafikStatusPengiriman" src/pages/Laporan.jsx` >= 2 and `grep -c "GrafikTrenPermintaan" src/pages/Laporan.jsx` >= 2 (both chart components present and used).
</verification>

<success_criteria>
- REPORT-01: Manajer Distribusi sees decision/ranking-focused data (Riwayat Keputusan table + Tren Permintaan chart, unchanged from current behavior) — satisfied by Task 2's `!isTimLogistik` branch.
- REPORT-02: Tim Logistik sees status/delivery-focused data (Distribusi Aktif table with armada/ETA + Status Pengiriman doughnut chart) — satisfied by Task 1's data layer + Task 2's `isTimLogistik` branch.
- REPORT-03: The two views read from materially different store collections (`keputusan` vs `riwayatKeputusan`/`permintaan`) and render structurally different sections, not a relabeled heading — satisfied by `filteredKeputusan` (Task 1) being a distinct source from `filteredRiwayat`/`filteredPermintaan`, and by the doughnut chart being a distinct visualization from the line chart.
</success_criteria>

<output>
Create `.planning/phases/02-role-differentiated-reporting/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Notes |
|--------|------|------|-------|
| `isTimLogistik` | const (boolean) | src/pages/Laporan.jsx | `roleAktif === "Tim Logistik"`, drives all role branching |
| `statusLabels` | module-level const (object) | src/pages/Laporan.jsx | `{ menunggu, "dalam-pengiriman", selesai }` label map, mirrors StatusDistribusi.jsx |
| `filteredKeputusan` | useMemo (array) | src/pages/Laporan.jsx | `snapshot.keputusan` filtered by periode range, sorted desc by tanggal_keputusan |
| `statusCounts` | useMemo (object) | src/pages/Laporan.jsx | `{ menunggu, "dalam-pengiriman", selesai }` counts from filteredKeputusan |
| `tableRowsManajer` | derived array | src/pages/Laporan.jsx | Renamed from existing `tableRows`; feeds Manajer Distribusi Tabel |
| `tableRowsTimLogistik` | derived array | src/pages/Laporan.jsx | New row shape with `armada` column; feeds Tim Logistik Tabel |
| `GrafikStatusPengiriman` | function component | src/pages/Laporan.jsx | New doughnut chart component, `{ counts }` prop, chart.js/auto lifecycle pattern |
| `handleExportCsv` (modified) | function | src/pages/Laporan.jsx | Branches on `isTimLogistik` for filename + column shape |
| Page heading/description (modified) | JSX | src/pages/Laporan.jsx | `judul`/`deskripsi` props on `<PageHeader>` now role-conditional |

