---
phase: 06-backend-skeleton-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - server/package.json
  - server/.env
  - server/.env.example
  - server/.gitignore
  - server/prisma/schema.prisma
  - .gitignore
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "A local PostgreSQL instance is reachable on a known port via Docker, independent of any native psql install"
    - "Prisma schema models all 7 data domains with foreign-key relationships matching today's localStorage shapes"
    - "Running a Prisma migration against the fresh Postgres container creates real tables with FK constraints, queryable via Prisma Studio or psql"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local Postgres 16 container definition for dev"
      contains: "postgres:16"
    - path: "server/package.json"
      provides: "Backend package manifest, separate from src/ frontend, with Express 5 + Prisma 6.19.2 + jsonwebtoken + bcryptjs deps"
      contains: "\"prisma\""
    - path: "server/prisma/schema.prisma"
      provides: "All 7 data domain models (Akun, Kota, Permintaan, Keputusan, RiwayatKeputusan, ActivityLog, Notifikasi) with relations"
      contains: "model Akun"
  key_links:
    - from: "server/prisma/schema.prisma"
      to: "docker-compose.yml"
      via: "DATABASE_URL in server/.env points at the Postgres container's mapped host port"
      pattern: "postgresql://"
---

<objective>
Stand up the backend project skeleton: a local Dockerized PostgreSQL instance for development, a new `server/` Node.js project (separate `package.json` from the Vite frontend per the locked backend-stack decision), and a complete Prisma schema modeling all 7 existing data domains (`akun`, `daftarKota`, `permintaan`, `keputusan`, `riwayatKeputusan`, `activityLog`, `notifikasi`) with correct foreign-key relationships — migrated into a real database, with zero rows of data yet.

Purpose: Every later phase (7-10) depends on this schema and a running database existing. Getting field names, types, and relations wrong here cascades expensive rework into auth, CRUD, and frontend-integration phases.
Output: `docker-compose.yml` (Postgres dev container), `server/package.json` + lockfile, `server/prisma/schema.prisma` (7 models + relations), an applied initial migration (`server/prisma/migrations/`), and a verified Prisma Client connection to the live container.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Current client-side data shapes this schema must be able to back (read, do not modify)
@src/store.js
@src/data/permintaan.json
@src/data/keputusan.json
@src/data/notifikasi.json
@src/data/activityLog.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose Postgres + server/ project scaffold</name>
  <files>docker-compose.yml, server/package.json, server/.env, server/.env.example, server/.gitignore, .gitignore</files>
  <read_first>
    - C:\Users\acer\Switera\package.json (existing frontend manifest — match double-quote/2-space style, but this is a SEPARATE package.json, not a workspace merge)
    - C:\Users\acer\Switera\.gitignore (existing root ignore rules — add server-specific entries, do not remove existing lines)
  </read_first>
  <action>
    Create `docker-compose.yml` at the repo root defining one service `db` using image `postgres:16`, with environment `POSTGRES_USER=switera`, `POSTGRES_PASSWORD=switera_dev_password`, `POSTGRES_DB=switera`, port mapping `"5432:5432"`, and a named volume `switera_pgdata:/var/lib/postgresql/data` for persistence across container restarts. Add a top-level `volumes:` key declaring `switera_pgdata`. This is dev-only — document with a one-line YAML comment that the password is a local-dev default, not for production.

    Create `server/package.json` with `"name": "switera-server"`, `"private": true`, `"version": "1.0.0"`, `"type": "module"`, and `"scripts"`: `"dev"` (run `node --watch index.js` — no nodemon dependency needed, Node 18+ has `--watch` built in), `"start"` (`node index.js`), `"prisma:migrate"` (`prisma migrate dev`), `"prisma:generate"` (`prisma generate`), `"prisma:studio"` (`prisma studio`), `"db:seed"` (`node prisma/seed.js`). Dependencies (use these exact versions — do not let npm resolve newer majors): `express` `^5.0.0`, `@prisma/client` `6.19.2`, `jsonwebtoken` `^9.0.2`, `bcryptjs` `^2.4.3`, `cors` `^2.8.5`, `zod` `^3.23.8`, `dotenv` `^16.4.5`. DevDependencies: `prisma` `6.19.2` (MUST match `@prisma/client` exactly — Prisma requires matching major.minor.patch between client and CLI). Run `npm install` inside `server/` after writing the file to generate `server/package-lock.json`.

    Create `server/.env` with `DATABASE_URL="postgresql://switera:switera_dev_password@localhost:5432/switera?schema=public"` and a placeholder `JWT_SECRET="dev-secret-change-in-phase-7"` (Phase 7 will use this var; declaring it now avoids a later schema/env split). Create `server/.env.example` mirroring the same keys with non-secret placeholder values (e.g. `DATABASE_URL="postgresql://user:password@localhost:5432/switera?schema=public"`).

    Create `server/.gitignore` containing `node_modules/`, `.env`, `prisma/migrations/.tmp/` (Prisma scratch dir), and `*.log`.

    Edit the root `.gitignore` (existing file, do not overwrite) to append a new section for the backend: `server/node_modules/` is already covered by a recursive `node_modules/` rule already present at the root — confirm this before adding a duplicate. Do NOT add `server/.env` to the root `.gitignore` since `server/.gitignore` already covers it; avoid redundant rules across the two files.

    Start the container: run `docker compose up -d db` from the repo root. Wait for the container healthcheck or a fixed few seconds, then confirm Postgres is accepting connections.
  </action>
  <verify>
    <automated>docker compose ps --format json | grep -c '"State":"running"'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d db` succeeds with no error and `docker compose ps` shows the `db` service in a running/healthy state
    - `server/package.json` exists with `@prisma/client` and `prisma` both pinned to exactly `6.19.2` (verify via `grep '"prisma"' server/package.json` and `grep '"@prisma/client"' server/package.json` both showing `6.19.2`, not a caret range)
    - `server/node_modules/` exists after `npm install` (confirms install succeeded)
    - `server/.env` contains a `DATABASE_URL` pointing at `localhost:5432` matching the docker-compose port mapping
    - Root `.gitignore` still contains all pre-existing lines unchanged (no destructive overwrite)
  </acceptance_criteria>
  <done>Postgres 16 container is running locally via Docker Compose on port 5432; `server/` has its own installed `package.json` with Express 5, Prisma 6.19.2 (client+CLI matched), jsonwebtoken, bcryptjs, cors, zod, dotenv; `.env`/`.env.example` exist with a working `DATABASE_URL`.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma schema — all 7 data domains with relations, migrated</name>
  <files>server/prisma/schema.prisma, server/prisma/migrations/</files>
  <read_first>
    - C:\Users\acer\Switera\src\store.js (lines 1-114 for seed shapes; lines 213-330 for Akun/Kota field usage and the cascade-rename/block-delete logic this schema must support relationally; lines 362-511 for Permintaan/Keputusan/RiwayatKeputusan field usage)
    - C:\Users\acer\Switera\src\data\permintaan.json (Permintaan field names/types: id string like "PMT-001", kota string, tanggal_permintaan date-string, tanggal_input date-string, jumlah_permintaan number, keterangan string)
    - C:\Users\acer\Switera\src\data\keputusan.json (Keputusan field names: id string like "KPT-001", kota_tujuan string, volume_tbs number, tanggal_keputusan date-string, diputuskan_oleh string, status enum-like string: "menunggu"|"dalam-pengiriman"|"selesai", optional waktu_menunggu/waktu_dalam-pengiriman/waktu_selesai/waktu_dibatalkan timestamps per store.js:438,462)
    - C:\Users\acer\Switera\src\data\notifikasi.json (Notifikasi fields: id "NTF-001", judul, pesan, tipe enum "info"|"warning"|"success", dibaca boolean, waktu ISO timestamp)
    - C:\Users\acer\Switera\src\data\activityLog.json (ActivityLog fields per store.js:164-175: id "LOG-001", aktor string, role string, aksi string, waktu ISO timestamp)
  </read_first>
  <action>
    Create `server/prisma/schema.prisma` with `generator client { provider = "prisma-client-js" }` and `datasource db { provider = "postgresql"; url = env("DATABASE_URL") }`.

    Define exactly 7 models. Use plain Prisma defaults — PascalCase model names map to PascalCase Postgres table names (e.g. `model Akun` creates table `"Akun"`), camelCase field names map to camelCase columns (e.g. `kotaNama` creates column `"kotaNama"`). Do NOT add `@@map`/`@map` directives — there is no requirement for snake_case database identifiers, and skipping them keeps the schema, the seed script (Plan 02), and the service layer (Plan 03) all referencing the exact same camelCase names with zero translation layer:

    `model Akun` — `id String @id` (keep the existing "U001" style string IDs, do not switch to autoincrement int — store.js's `getNextId` pattern depends on string prefix+number IDs and Phase 8 will reuse this same convention server-side), `nama String`, `username String @unique`, `password String` (will hold the bcrypt hash, not plaintext — column name stays `password` per existing convention, semantic change documented in a schema comment `// bcrypt hash, never plaintext (see DATA-02)`), `role String` (no Prisma enum — keep as String since role values ("Admin", "Manajer Distribusi", "Tim Logistik") are Indonesian display strings already used as-is in `src/utils/navigation.js`; do not introduce a new enum type the frontend doesn't know about), `createdAt DateTime @default(now())`.

    `model Kota` — `nama String @id` (city name is the natural key today — `store.js` has no separate city ID, `tambahKota`/`updateKota`/`hapusKota` all key off `nama`; preserve this so the cascade-rename behavior in DATA-03 maps directly), `kapasitas Int`, with back-relations `permintaan Permintaan[]`, `keputusan Keputusan[]`, `riwayatKeputusan RiwayatKeputusan[]`.

    `model Permintaan` — `id String @id` (e.g. "PMT-001"), `kotaNama String` (FK column), `kota Kota @relation(fields: [kotaNama], references: [nama])`, `tanggalPermintaan String` (keep as String matching the existing `"2026-05-20"` date-string format already used by `formatDate` in `src/utils/format.js` — do not switch to `DateTime` and force a parsing/timezone migration the frontend doesn't expect yet), `tanggalInput String`, `jumlahPermintaan Float` (tonase can be fractional), `keterangan String?` (optional, matches occasional empty values), `createdAt DateTime @default(now())`. Add `@@index([kotaNama])`.

    `model Keputusan` — `id String @id` (e.g. "KPT-001"), `kotaTujuanNama String`, `kotaTujuan Kota @relation(fields: [kotaTujuanNama], references: [nama])`, `volumeTbs Float`, `tanggalKeputusan String`, `diputuskanOleh String`, `status String` (values: "menunggu" | "dalam-pengiriman" | "selesai" — keep as String, not Prisma enum, since `removeKeputusan` in store.js also writes the non-listed value "dibatalkan" into status on cancel; an enum would reject that transition), `waktuMenunggu DateTime?`, `waktuDalamPengiriman DateTime?`, `waktuSelesai DateTime?`, `waktuDibatalkan DateTime?`. Add `@@index([kotaTujuanNama])`.

    `model RiwayatKeputusan` — same shape as Keputusan (id, kotaTujuanNama/kotaTujuan relation, volumeTbs, tanggalKeputusan, diputuskanOleh, status, the four nullable waktu_* timestamps) — this mirrors `store.js`'s current behavior where `riwayatKeputusan` is a parallel array that retains cancelled/historical entries even after `keputusan` removes them (see `removeKeputusan` store.js:490-501, `addKeputusan` store.js:434-451 writing to both). Keep them as two distinct tables (not one table with a status flag) to match the existing two-array shape exactly — Phase 8's service layer will decide write-fanout, not this schema. Add `@@index([kotaTujuanNama])`.

    `model ActivityLog` — `id String @id` (e.g. "LOG-001"), `aktor String`, `role String`, `aksi String`, `waktu DateTime @default(now())`.

    `model Notifikasi` — `id String @id` (e.g. "NTF-001"), `judul String`, `pesan String`, `tipe String` (values: "info" | "warning" | "success" — keep String for the same reason as Keputusan.status), `dibaca Boolean @default(false)`, `waktu DateTime @default(now())`.

    Do not add a `Session` or `RefreshToken` model — AUTH-05/06 are explicitly deferred to v2 Requirements per ROADMAP.md/REQUIREMENTS.md; adding that table now is scope creep beyond DATA-01/02/03.

    After writing the schema, run `npx prisma migrate dev --name init` from inside `server/` (with the Docker container from Task 1 already running) to generate and apply the initial migration, then run `npx prisma generate` to produce the Prisma Client.
  </action>
  <verify>
    <automated>cd server && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` (run from `server/`) exits 0 with no schema errors
    - `server/prisma/migrations/` contains exactly one migration directory (e.g. `<timestamp>_init`) with a `migration.sql` that contains `CREATE TABLE` statements for all 7 tables
    - Running `npx prisma studio` (or `docker exec` + `psql -c '\dt'`) against the live container lists 7 tables
    - The generated migration SQL contains a `FOREIGN KEY` constraint referencing the Kota table for Permintaan, Keputusan, and RiwayatKeputusan (`grep -c 'FOREIGN KEY' server/prisma/migrations/*/migration.sql` returns 3 or more)
    - `server/node_modules/.prisma/client` exists (confirms `prisma generate` ran successfully)
  </acceptance_criteria>
  <done>All 7 models exist in `server/prisma/schema.prisma`, the initial migration is applied to the live Postgres container creating 7 real tables with FK constraints from Permintaan/Keputusan/RiwayatKeputusan to Kota, and Prisma Client is generated and importable from `server/node_modules/@prisma/client`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------|
| Dev machine -> Docker container | Local-only network boundary; Postgres port 5432 exposed to host loopback for dev convenience |
| `.env` file -> version control | Secrets (DB credentials, JWT secret placeholder) must never reach git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-01 | Information Disclosure | `server/.env` | mitigate | `server/.gitignore` excludes `.env`; only `.env.example` with placeholder values is committed |
| T-06-02 | Information Disclosure | `docker-compose.yml` hardcoded dev password | accept | Dev-only local container, not internet-exposed, password documented as local-dev default in a code comment; production secrets are out of scope for this school-project milestone |
| T-06-03 | Tampering | Prisma schema field types (e.g. status/tipe as String not enum) | accept | Deliberate per-field decision to match existing store.js write patterns (e.g. "dibatalkan" status); validation enforcement is deferred to Phase 8's Zod layer (API-02), not this phase |
| T-06-SC | Tampering | npm installs (express, @prisma/client, jsonwebtoken, bcryptjs, cors, zod, dotenv, prisma) | mitigate | All packages are widely-used, long-established (express, jsonwebtoken, bcryptjs, cors, zod, dotenv, prisma/@prisma-client) with no legitimacy concern — see Package Legitimacy note below |

**Package Legitimacy Note:** No RESEARCH.md exists for this phase (research skipped per `research_enabled=false`), so the formal Package Legitimacy Audit table is not available. All 8 packages in this plan (`express`, `@prisma/client`, `prisma`, `jsonwebtoken`, `bcryptjs`, `cors`, `zod`, `dotenv`) are top-50 npm downloads-per-week packages with multi-year histories and no known supply-chain incidents — treating as de facto verified rather than `[ASSUMED]`/`[SUS]`. If any future phase introduces a new, less-common package, a blocking human-verify checkpoint must precede its install per the Package Legitimacy Gate protocol.
</threat_model>

<verification>
1. `docker compose ps` shows `db` running and healthy.
2. `cd server && npx prisma validate` exits 0.
3. `cd server && npx prisma studio` (or psql) shows 7 tables: Akun, Kota, Permintaan, Keputusan, RiwayatKeputusan, ActivityLog, Notifikasi — all empty (0 rows), confirming schema-only migration with no data yet (seeding is Plan 02's job).
4. `grep -c 'FOREIGN KEY' server/prisma/migrations/*/migration.sql` returns 3+ (Permintaan, Keputusan, RiwayatKeputusan -> Kota).
</verification>

<success_criteria>
- Postgres running locally via Docker on port 5432, durable across restarts (named volume).
- `server/` is a fully independent Node.js project with its own `package.json`, pinned Prisma 6.19.2 (client+CLI matched, not 7.x).
- All 7 data domains modeled in `schema.prisma` with field names/types traceable to current `store.js`/`*.json` shapes.
- Initial migration applied; FK constraints from Permintaan/Keputusan/RiwayatKeputusan to Kota exist at the database level (this is the DB-level half of DATA-03 — service-layer enforcement is Plan 03).
</success_criteria>

<output>
Create `.planning/phases/06-backend-skeleton-data-model/06-01-SUMMARY.md` when done
</output>
