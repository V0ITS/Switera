---
phase: 06-backend-skeleton-data-model
plan: 01
subsystem: database
tags: [postgres, prisma, docker, express, schema, backend-skeleton]

# Dependency graph
requires: []
provides:
  - Local Postgres 16 dev container via Docker Compose, durable across restarts (named volume)
  - Independent server/ Node.js project (package.json, Express 5, Prisma 6.19.2 client+CLI matched, jsonwebtoken, bcryptjs, cors, zod, dotenv)
  - server/.env + server/.env.example with DATABASE_URL and JWT_SECRET placeholder
  - Prisma schema modeling all 7 data domains (Akun, Kota, Permintaan, Keputusan, RiwayatKeputusan, ActivityLog, Notifikasi)
  - Applied initial migration with FK constraints from Permintaan/Keputusan/RiwayatKeputusan to Kota
  - Generated Prisma Client at server/node_modules/@prisma/client
affects: [07-auth, 08-domain-crud, 09-frontend-integration, 10-multi-client-sync]

# Tech tracking
tech-stack:
  added: [postgres:16 (docker), express@^5.0.0, "@prisma/client@6.19.2", prisma@6.19.2 (devDep), jsonwebtoken@^9.0.2, bcryptjs@^2.4.3, cors@^2.8.5, zod@^3.23.8, dotenv@^16.4.5]
  patterns:
    - "server/ is a fully separate Node.js project from the Vite frontend (own package.json, own node_modules, own .gitignore) — not a monorepo workspace"
    - "Prisma models use plain camelCase field names / PascalCase table names with zero @@map/@map translation layer, mirroring src/store.js field names 1:1 (e.g. jumlahPermintaan, kotaTujuanNama)"
    - "City (Kota) keeps its existing natural key (nama as @id) instead of introducing a surrogate integer ID, preserving store.js's name-keyed cascade-rename/block-delete semantics at the DB level via ON UPDATE CASCADE / ON DELETE RESTRICT"
    - "Enum-like fields (status, tipe) stored as plain String, not Prisma enum, because store.js writes values outside the documented set (e.g. removeKeputusan writes 'dibatalkan' into status) — enum enforcement deferred to Phase 8's Zod validation layer"

key-files:
  created:
    - docker-compose.yml
    - server/package.json
    - server/package-lock.json
    - server/.env
    - server/.env.example
    - server/.gitignore
    - server/prisma/schema.prisma
    - server/prisma/migrations/20260624133416_init/migration.sql
    - server/prisma/migrations/migration_lock.toml
  modified: []

key-decisions:
  - "Pinned prisma and @prisma/client to exactly 6.19.2 (not the offered 7.8.0 major) per plan requirement that client and CLI versions match exactly"
  - "Declined npm audit fix --force for the 3 high-severity transitive vulnerabilities in Prisma's effect dependency — the suggested fix bumps prisma to 6.19.3, breaking the plan's exact-pin requirement; accepted per threat model T-06-SC disposition (well-established package, no direct exploit path in our usage)"
  - "Left root .gitignore unmodified — existing recursive node_modules/ rule already covers server/node_modules/, and server/.gitignore already covers server/.env, so no new rule was needed without creating redundancy"
  - "FK constraints generated by Prisma default to ON UPDATE CASCADE / ON DELETE RESTRICT, which happens to align exactly with store.js's existing updateKota cascade-rename and hapusKota block-delete-if-referenced behavior — no extra schema annotation needed to get this"

patterns-established:
  - "Backend dev environment is fully Dockerized for Postgres; no native psql/Postgres install required on the dev machine"
  - "Date-only fields (tanggalPermintaan, tanggalInput, tanggalKeputusan) stay as String matching the existing 'YYYY-MM-DD' format from src/utils/format.js, deferring any DateTime/timezone migration to a later phase"

requirements-completed: [DATA-01]

# Metrics
duration: 4min
completed: 2026-06-24
status: complete
---

# Phase 6 Plan 1: Backend Skeleton & Data Model Summary

**Dockerized Postgres 16 + independent server/ Node.js project + Prisma schema modeling all 7 store.js data domains, migrated into a live database with FK constraints from Permintaan/Keputusan/RiwayatKeputusan to Kota.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-24T13:31:05Z
- **Completed:** 2026-06-24T13:35:32Z
- **Tasks:** 2
- **Files modified:** 9 created, 0 modified

## Accomplishments

- Local Postgres 16 running via Docker Compose on port 5432, with a named volume (`switera_pgdata`) so data survives container restarts
- `server/` stood up as a fully independent Node.js project (separate `package.json`/`node_modules` from the Vite frontend) with Express 5, Prisma 6.19.2 (client + CLI exactly matched), jsonwebtoken, bcryptjs, cors, zod, dotenv installed
- `server/prisma/schema.prisma` models all 7 existing data domains (Akun, Kota, Permintaan, Keputusan, RiwayatKeputusan, ActivityLog, Notifikasi) with field names/types traced directly to `src/store.js` and `src/data/*.json`
- Initial migration (`20260624133416_init`) applied to the live container: 7 real tables created, all 0 rows, with 3 FK constraints (Permintaan, Keputusan, RiwayatKeputusan → Kota) verified via `\dt` and direct row-count query
- Prisma Client generated and importable from `server/node_modules/@prisma/client`

## Task Commits

Each task was committed atomically:

1. **Task 1: Docker Compose Postgres + server/ project scaffold** - `39a183e` (feat)
2. **Task 2: Prisma schema — all 7 data domains with relations, migrated** - `0dd0de0` (feat)

**Plan metadata:** skipped (commit_docs: false — see Notes below)

## Files Created/Modified

- `docker-compose.yml` - Postgres 16 dev container definition with named volume for persistence
- `server/package.json` - Backend manifest: Express 5, Prisma 6.19.2 (client+CLI matched), jsonwebtoken, bcryptjs, cors, zod, dotenv
- `server/package-lock.json` - Locked dependency tree from `npm install`
- `server/.env` - DATABASE_URL pointing at the local container, JWT_SECRET placeholder for Phase 7 (gitignored, not committed)
- `server/.env.example` - Non-secret placeholder mirror of `.env`
- `server/.gitignore` - Excludes node_modules/, .env, Prisma scratch dirs, logs
- `server/prisma/schema.prisma` - All 7 models with relations, indexes on FK columns
- `server/prisma/migrations/20260624133416_init/migration.sql` - Initial migration creating 7 tables + 3 FK constraints
- `server/prisma/migrations/migration_lock.toml` - Prisma migration provider lock file

## Decisions Made

- Pinned `prisma`/`@prisma/client` to exactly `6.19.2`, declining the offered `7.8.0` major upgrade and the `npm audit fix --force` path (which would have bumped to `6.19.3`, also breaking the exact-pin requirement) — see threat model T-06-SC, accepted as a known, well-established transitive issue
- Left the root `.gitignore` untouched since existing rules already fully cover the new `server/node_modules/` and `server/.env` paths without duplication
- Kept `Kota.nama` as the natural-key `@id` (no surrogate integer ID), preserving `store.js`'s existing cascade-rename/block-delete logic 1:1 at the database level

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. `npm install` reported 3 high-severity audit findings in a transitive Prisma dependency (`effect`, via `@prisma/config`) — investigated and confirmed the only available fix breaks the plan's explicit exact-version-pin requirement, so left as-is per the threat model's accept disposition for T-06-SC. Not a deviation from the plan since the plan explicitly mandated the exact pinned versions.

## User Setup Required

None - no external service configuration required. Docker Desktop and Node.js were already available in the execution environment per the orchestrator's environment notes.

## Next Phase Readiness

- Database schema is live and ready for Plan 02 (seeding) and Plan 03 (service layer / Phase 8 CRUD)
- FK constraints (`ON UPDATE CASCADE` / `ON DELETE RESTRICT` to Kota) are already DB-enforced, ready for Phase 8's service-layer write-fanout logic to build on top of
- `JWT_SECRET` env var already declared in `server/.env`/`.env.example` for Phase 7 (auth) to consume without a schema/env split
- No blockers identified

## Self-Check: PASSED

- FOUND: docker-compose.yml
- FOUND: server/package.json
- FOUND: server/prisma/schema.prisma
- FOUND: server/prisma/migrations/20260624133416_init/migration.sql
- FOUND: 39a183e (in git log)
- FOUND: 0dd0de0 (in git log)
- docker compose ps: `db` service `State: running`
- prisma validate: schema valid, exit 0
- 7 tables confirmed in live container via `\dt`, all 0 rows
- 3 FOREIGN KEY constraints confirmed in migration.sql, all referencing Kota("nama")

---
*Phase: 06-backend-skeleton-data-model*
*Completed: 2026-06-24*
