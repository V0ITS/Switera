---
phase: 10-multi-client-sync-cross-role-uat
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - server/src/sync/multiClientSync.verify.mjs
  - server/src/sync/multiClientRbac.verify.mjs
  - server/src/sync/multiClientRace.verify.mjs
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "A change made by one simulated client session is observed by a second simulated client session within one poll interval, proven through the real store.js polling loop (not a hand-rolled fetch in the test)"
    - "Re-running the Phase 7 RBAC denial through a polling-active multi-client session still returns 403 — polling introduced no new route that skips requireRole"
    - "Re-running the Phase 8 concurrent-approval race through two real logged-in client sessions (not direct curl) still yields exactly one success and one 409 per contested decision"
    - "Each verification script is idempotent and self-cleaning (creates throwaway rows, removes them in a finally block) so it is safely re-runnable, matching the established *.verify.mjs pattern"
  artifacts:
    - path: "server/src/sync/multiClientSync.verify.mjs"
      provides: "Two store.js client instances; client A mutates, client B observes via its poll tick within one interval (SYNC-01 convergence proof)"
      contains: "startPolling"
    - path: "server/src/sync/multiClientRbac.verify.mjs"
      provides: "RBAC 403 re-verification under polling-active concurrent sessions (Success Criterion 3)"
      contains: "403"
    - path: "server/src/sync/multiClientRace.verify.mjs"
      provides: "LOGIC-02 concurrent-approval re-verification via two real client sessions (Success Criterion 4)"
      contains: "409"
  key_links:
    - from: "server/src/sync/multiClientSync.verify.mjs"
      to: "src/store.js"
      via: "imports the real store + startPolling/stopPolling; client B's poll tick (not a bespoke fetch) is what observes client A's change"
      pattern: "startPolling"
    - from: "the verify scripts"
      to: "the live Express :4000 backend"
      via: "store.js → apiClient.js → real HTTP, driven by VITE_API_BASE_URL against a started app instance"
      pattern: "apiFetch|/auth/login"
---

<objective>
Re-verify the Phase 7 RBAC guarantee and the Phase 8 LOGIC-02 concurrency guarantee under MULTI-CLIENT POLLING LOAD, and prove SYNC-01 convergence — the actual deliverable of this phase per its goal statement ("the authorization/concurrency guarantees from earlier phases hold up under real cross-role use").

The novel value here is NOT re-running the existing single-client `keputusanRoutes.verify.mjs` unchanged (it already passes). It is proving that the polling loop added in 10-01 did not introduce a regression: that concurrent client sessions actively polling against the live backend still observe (a) exactly-one-winner on the contested decision-approval path, (b) a 403 on a disallowed-role mutation, and (c) cross-client data convergence within one poll interval — all driven through the REAL client code path (store.js → apiClient.js → live Express API), not bespoke test-only fetches.

Purpose: Close SYNC-01 with evidence, and re-confirm Success Criteria 3 and 4 of Phase 10 under the conditions that actually matter (polling active, multiple sessions), so the v2.0 milestone can ship.

Output: Three idempotent, self-cleaning `*.verify.mjs` scripts under `server/src/sync/`, each printing PASS/FAIL report lines and exiting 0 on success, matching the established Phase 6-8 verification-script convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# The polling subsystem this plan exercises (10-01 output).
@src/store.js
@src/api/apiClient.js

# The exact verification-script conventions to replicate: app.listen(0)
# ephemeral port + built-in fetch + report()/exitCode + idempotent finally
# cleanup. Seed accounts: admin/admin123 (Admin), manajer/manajer123
# (Manajer Distribusi), logistik/logistik123 (Tim Logistik).
@server/src/routes/keputusanRoutes.verify.mjs
@server/src/services/keputusanService.race.verify.mjs
@server/src/middleware/requireRole.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Multi-client SYNC-01 convergence verify script driving the real polling loop</name>
  <files>server/src/sync/multiClientSync.verify.mjs</files>
  <read_first>
    - server/src/routes/keputusanRoutes.verify.mjs lines 22-50: the `app.listen(0)` ephemeral-port bootstrap, the `login(baseUrl, username, password)` helper, and the LOGIN_SETUP_OK / report() pattern. Replicate this bootstrap so the script never collides with the running :4000 dev server and is re-runnable.
    - src/store.js lines 237-248 (login), 303-318 (hydrate), 320-331 (loadPermintaan / getPermintaan), 448-464 (addPermintaan), 480-489 (removePermintaan), and the 10-01 additions startPolling/stopPolling/POLL_INTERVAL_MS. These are the real client methods the script must drive.
    - src/api/apiClient.js lines 9 (API_BASE_URL from VITE_API_BASE_URL) and 66-72: the script must point the client at the ephemeral-port app via env, since the store imports a single shared apiClient.
    - IMPORTANT ARCHITECTURE CONSTRAINT: src/store.js is a singleton — importing it once gives ONE cache and ONE token slot. To simulate TWO independent client sessions in one Node process, use Node's `import()` with a cache-busting query (e.g. `await import('../../../src/store.js?client=A')` and `?client=B`) so each gets its own module instance, OR run two child Node processes. Choose ONE approach and document why. The cache-bust-import approach is preferred (single process, simpler cleanup) — verify it actually yields two independent `store` objects before relying on it (log `clientA.store !== clientB.store`).
  </read_first>
  <action>
    Create `server/src/sync/multiClientSync.verify.mjs`. It must prove SYNC-01: a change by client A is observed by client B via B's POLL TICK (not a manual fetch), within one poll interval.

    Bootstrap: start the Express app on an ephemeral port exactly as keputusanRoutes.verify.mjs does (`const server = app.listen(0)`; await listening; derive `baseUrl`). Set `process.env.VITE_API_BASE_URL = baseUrl` BEFORE importing store.js so apiClient's `API_BASE_URL` resolves to the ephemeral app. (Note: apiClient reads `import.meta.env?.VITE_API_BASE_URL` — under plain Node `import.meta.env` is undefined, so it falls back to `http://localhost:4000`; the running dev server IS on 4000, which is acceptable, but to keep the test hermetic against the EPHEMERAL app, instead start the app on the ephemeral port AND also rely on the real :4000 only if the dev server is guaranteed up. SIMPLER+hermetic choice: do NOT start an ephemeral app here — point the clients at the assumed-running `http://localhost:4000` dev backend, the same target the real browser uses. Document this choice: it is the most faithful simulation of two real browser tabs, both hitting :4000. Guard with a preflight `GET /health` and abort with a clear message if :4000 is not reachable.)

    Use the cache-busting dynamic-import approach to get two independent store singletons: `const { store: storeA } = await import('../../../src/store.js?client=A')` and likewise `?client=B`. Assert `storeA !== storeB` (report `TWO_INDEPENDENT_CLIENTS_OK`); if they are the same object the cache-bust failed — fall back to the documented child-process approach or fail loudly.

    Provide a global localStorage shim before importing store.js (Node has no `window.localStorage`): set `globalThis.window = { localStorage: <in-memory Map-backed impl with getItem/setItem/removeItem> , matchMedia: () => ({ matches:false }) }`. NOTE the two clients share `globalThis.window.localStorage` — but each store instance stores its JWT under the SAME `switera_token` key via apiClient, so the two clients would clobber each other's token. To keep tokens independent, give each client its OWN localStorage object by setting `globalThis.window.localStorage` to client A's store, importing A, then swapping to client B's store before importing B — OR simpler: since apiClient.getToken reads at call time, run the test so that you control which token is active by NOT relying on the shared store for tokens; instead log in each client and capture its token, then for the convergence assertion drive client B's loadPermintaan with B's token. Document whichever isolation approach is used; the acceptance criterion is only that A's mutation and B's observation use DIFFERENT authenticated sessions.

    Scenario:
    1. Log in client A as `manajer`/`manajer123` and client B as `logistik`/`logistik123` (two different roles, both real sessions). Report `LOGIN_BOTH_CLIENTS_OK`.
    2. Hydrate both, then `storeB.startPolling()` so B is polling on the real 4000ms interval.
    3. Record B's current permintaan count (`storeB.getPermintaan().length`).
    4. Through client A, `await storeA.addPermintaan({...})` a throwaway permintaan (use a recognizable `keterangan` marker like `__SYNC_VERIFY_TEMP__` and a valid existing kota — read one from `storeA.getDaftarKota()` after hydrate). Capture the created id.
    5. WAIT for B's poll loop to pick it up: poll-wait up to ~3 intervals (e.g. 13s) checking `storeB.getPermintaan().some(p => p.id === createdId)` every 500ms. Report `SYNC_CONVERGENCE_OK` true if B observes the new row WITHOUT any manual loadPermintaan call on B (B saw it purely because its interval ticked). Include the elapsed ms in the detail. Report FAIL with the elapsed time if it never converges.
    6. `storeB.stopPolling()`. Assert (report `POLL_STOPPED_OK`) that after stopPolling, a further mutation by A is NOT observed by B within one interval — i.e. stopPolling actually halts sync (drive A to remove+re-add or add a second marker row, wait one interval, confirm B did NOT pick it up). This proves the stop path, the same mechanism that fires on logout/401.
    7. Cleanup in `finally`: remove every throwaway permintaan created (by id, via storeA.removePermintaan), stopPolling on both clients, close any handles. Make the whole script idempotent/re-runnable.

    Print a final `MULTI_CLIENT_SYNC_OK` aggregating all sub-checks; set process.exitCode accordingly. Add a top-of-file comment honestly stating: this proves convergence through the REAL store polling loop against the live backend, simulating two concurrent sessions in one Node process; it is NOT a rendered two-browser-tab pixel check (no Playwright/chromium-cli available — carried-forward blocker), which remains a manual verification step recorded in the SUMMARY.
  </action>
  <acceptance_criteria>
    - File `server/src/sync/multiClientSync.verify.mjs` exists and runs under plain `node`.
    - It obtains TWO independent store instances (asserts `storeA !== storeB`) using two DIFFERENT authenticated role sessions (manajer + logistik).
    - The convergence assertion (`SYNC_CONVERGENCE_OK`) passes ONLY because client B's polling interval ticked — B never calls loadPermintaan/hydrate manually after A's mutation.
    - Concrete interval/timeout values are present: poll interval 4000ms (from store), convergence wait capped at ~13000ms checked every 500ms.
    - A `POLL_STOPPED_OK` check proves `stopPolling()` actually halts further sync (B does not observe a post-stop mutation within one interval).
    - The script self-cleans every throwaway row in a `finally` block and is re-runnable (running it twice yields identical PASS output).
    - Top-of-file comment honestly scopes this as script-level two-session proof, NOT a two-browser-tab pixel proof.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/server" && node src/sync/multiClientSync.verify.mjs 2>&1 | tail -20 | grep -q "MULTI_CLIENT_SYNC_OK true"</automated>
  </verify>
  <done>A standalone script proves, through the real store polling loop, that a mutation by one authenticated client session is observed by a second polling client session within one poll interval, and that stopPolling halts further sync — self-cleaning and re-runnable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Multi-client RBAC re-verification under polling (Success Criterion 3)</name>
  <files>server/src/sync/multiClientRbac.verify.mjs</files>
  <read_first>
    - server/src/routes/keputusanRoutes.verify.mjs lines 68-80 (the `KEPUTUSAN_POST_RBAC_DENY_OK` 403 check for a logistik token on POST /keputusan) — this is the exact denial to re-prove, but now with a polling client active in the same process. (The original Phase 7 `rbac.verify.mjs` no longer exists — it was deleted in 08-06 alongside the retired `/protected` demo router; `keputusanRoutes.verify.mjs` is the current equivalent.)
    - src/store.js: login, startPolling/stopPolling, addPermintaan/addKeputusan. The disallowed mutation to re-test is a Tim Logistik session attempting POST /keputusan (logistik is NOT allow-listed on POST per keputusanRoutes.js lines 37-41).
    - Task 1's localStorage shim + cache-bust import + preflight-:4000 approach — reuse the SAME helper conventions (consider factoring a tiny shared helper, but a self-contained copy per script is also acceptable and matches the existing one-file-per-verify convention).
  </read_first>
  <action>
    Create `server/src/sync/multiClientRbac.verify.mjs`. Goal: re-prove the Phase 7 RBAC 403 denial holds while polling is ACTIVE across concurrent sessions — confirming 10-01 introduced no new route and relaxed no requireRole check.

    Reuse Task 1's bootstrap conventions: localStorage/window shim, preflight `GET http://localhost:4000/health` (abort with a clear message if the dev backend is not up), and the cache-bust dynamic-import for independent store instances.

    Scenario:
    1. Client A: log in as `admin`/`admin123`. Client B: log in as `logistik`/`logistik123`. Report `RBAC_LOGIN_OK`.
    2. Start polling on BOTH clients (`storeA.startPolling()`, `storeB.startPolling()`) so the RBAC denial is exercised under live polling load, not in isolation.
    3. While both are polling, drive client B (Tim Logistik) to attempt a disallowed mutation: `await storeB.addKeputusan({ kota_tujuan: <a valid kota>, volume_tbs: 10, tanggal_keputusan: '2026-06-25', diputuskan_oleh: 'logistik', status: 'menunggu' })`. This MUST reject. Catch the thrown error and assert `error.status === 403` (apiClient sets `error.status`; see apiClient.js line ~116). Report `RBAC_POLLING_DENY_OK` true only on a 403. A success or any non-403 status is a FAIL (it would mean polling exposed an unguarded path).
    4. As a positive control, drive client A (Admin) to perform the SAME addKeputusan — it must SUCCEED (201 → returns a row with a KPT- id). Report `RBAC_POLLING_ALLOW_OK`. Capture the created id for cleanup.
    5. `finally`: stopPolling on both; remove the admin-created throwaway keputusan via `storeA.removeKeputusan(id)`; idempotent/re-runnable.

    Print `MULTI_CLIENT_RBAC_OK` aggregating the checks; set exitCode. Top-of-file comment: this re-runs the Phase 7/8 RBAC denial through the real client under active polling — the NEW assurance over the existing single-client keputusanRoutes.verify.mjs is specifically that polling added no bypass route.
  </action>
  <acceptance_criteria>
    - File `server/src/sync/multiClientRbac.verify.mjs` exists and runs under `node`.
    - Both clients are actively polling (startPolling called on each) when the disallowed mutation is attempted.
    - The Tim Logistik POST /keputusan attempt rejects with `error.status === 403` (`RBAC_POLLING_DENY_OK`).
    - The Admin POST /keputusan succeeds as a positive control (`RBAC_POLLING_ALLOW_OK`).
    - Self-cleans the admin-created throwaway row; re-runnable with identical PASS output.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/server" && node src/sync/multiClientRbac.verify.mjs 2>&1 | tail -20 | grep -q "MULTI_CLIENT_RBAC_OK true"</automated>
  </verify>
  <done>A standalone script proves a Tim Logistik session is still denied a POST /keputusan with 403 while polling is active across concurrent sessions, with an Admin positive control — confirming polling introduced no RBAC-skipping route (Success Criterion 3).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Multi-client concurrent-approval race re-verification (Success Criterion 4)</name>
  <files>server/src/sync/multiClientRace.verify.mjs</files>
  <read_first>
    - server/src/services/keputusanService.race.verify.mjs (the original LOGIC-02 race test at the service-call layer) and server/src/routes/keputusanRoutes.verify.mjs lines 126-164 (the HTTP-layer `KEPUTUSAN_PUT_RACE_OK` test: two concurrent PUT /keputusan/:id → exactly one 200, one 409). This task re-proves the SAME guarantee but driven through two REAL logged-in store.js client sessions (not direct fetch/curl, not the service layer).
    - server/src/services/keputusanService.js lines 219-272 (the optimistic-lock updateMany mechanism + the same-target-status 409 path) — the binding LOGIC-02 mechanism that must still hold.
    - src/store.js lines 545-562 (addKeputusan) and 571-584 (updateKeputusan — note it never writes the cache before the await resolves, so a 409 leaves the loser's cache untouched). The race is between two clients' `updateKeputusan` calls.
    - Task 1's bootstrap conventions (shim, preflight :4000, cache-bust import).
  </read_first>
  <action>
    Create `server/src/sync/multiClientRace.verify.mjs`. Goal: re-prove LOGIC-02 (exactly-one-winner on concurrent decision approval) through TWO real logged-in client sessions with polling active — the Phase 8 race test, re-run "with multiple real logged-in clients (not just direct curl)" as Success Criterion 4 demands.

    Reuse Task 1's bootstrap conventions (shim, preflight :4000, cache-bust import for two independent store instances).

    Scenario:
    1. Client A: log in as `manajer`/`manajer123`. Client B: log in as `logistik`/`logistik123` (both are allow-listed on PUT /keputusan per keputusanRoutes.js line 60). Report `RACE_LOGIN_OK`.
    2. Start polling on both clients so the race is exercised under live polling load.
    3. Client A creates one throwaway `menunggu` keputusan (`await storeA.addKeputusan({...})` with marker `diputuskan_oleh: '__RACE_VERIFY_TEMP__'`, a valid kota, status `menunggu`). Capture its id. Report `RACE_SETUP_OK`.
    4. Fire TWO genuinely concurrent status transitions to the SAME target status from the two DIFFERENT client sessions, via `Promise.allSettled([ storeA.updateKeputusan(id, { status: 'dalam-pengiriman' }), storeB.updateKeputusan(id, { status: 'dalam-pengiriman' }) ])`. Because updateKeputusan goes through runMutation → apiFetch → the real PUT route, this exercises the live optimistic-lock path through the client.
    5. Assert exactly one fulfilled and one rejected (`RACE_EXACTLY_ONE_WINNER_OK`). On the rejected one, assert the thrown error has `status === 409` and its message includes `sudah diperbarui` (`RACE_CONFLICT_SHAPE_OK`). (runMutation re-throws the apiClient error, which carries `.status` and the server's Indonesian message.)
    6. Confirm post-race server truth: have client A `await storeA.loadKeputusan()` and assert the row's status is `dalam-pengiriman` exactly once / not double-applied (`RACE_FINAL_STATE_OK`).
    7. `finally`: stopPolling both; `storeA.removeKeputusan(id)` the throwaway; idempotent/re-runnable.

    Print `MULTI_CLIENT_RACE_OK` aggregating the checks; set exitCode. Top-of-file comment: this re-runs the Phase 8 LOGIC-02 race through two real client sessions under active polling — the NEW assurance over keputusanRoutes.verify.mjs's direct-fetch race is that the FULL client stack (store.js → apiClient.js → backend), with polling running, still yields exactly one winner.
  </action>
  <acceptance_criteria>
    - File `server/src/sync/multiClientRace.verify.mjs` exists and runs under `node`.
    - The two concurrent approvals are issued from two DIFFERENT authenticated client store instances (manajer + logistik), both polling, via store.updateKeputusan (NOT direct fetch/curl).
    - Exactly one fulfilled + one rejected (`RACE_EXACTLY_ONE_WINNER_OK`); the rejection carries `status === 409` and the `sudah diperbarui` message (`RACE_CONFLICT_SHAPE_OK`).
    - Post-race the contested decision is in `dalam-pengiriman` exactly once (`RACE_FINAL_STATE_OK`) — no double-allocation.
    - Self-cleans the throwaway decision; re-runnable with identical PASS output.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/server" && node src/sync/multiClientRace.verify.mjs 2>&1 | tail -20 | grep -q "MULTI_CLIENT_RACE_OK true"</automated>
  </verify>
  <done>A standalone script proves two real concurrent logged-in client sessions approving the same decision under active polling still produce exactly one 200 and one 409, with the final state applied once — re-confirming LOGIC-02 holds across the full client stack (Success Criterion 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulated client session → live Express :4000 API | Each verify script drives real authenticated HTTP through store.js → apiClient.js, the same boundary a browser tab crosses |
| disallowed-role session → mutating route | Tim Logistik attempting POST /keputusan must be rejected at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10V-RBAC | Elevation of Privilege | RBAC under polling | mitigate | Task 2 actively re-proves the 403 denial with polling running on both clients; an Admin positive control rules out a false pass from an unrelated error. This is the executable evidence for Success Criterion 3 |
| T-10V-RACE | Tampering / double-allocation | concurrent decision approval | mitigate | Task 3 re-proves exactly-one-winner through two real client sessions under polling, asserting the 409 shape and the single final state — executable evidence for Success Criterion 4 |
| T-10V-LEAK | Information Disclosure | test data pollution | mitigate | Every script creates only marker-tagged throwaway rows and removes them in a `finally` block, so a failed/aborted run never leaves orphaned demo data in the shared Postgres; scripts are idempotent and re-runnable |
| T-10V-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages — scripts use only Node built-ins (`fetch`, dynamic `import`) and the existing store/apiClient. No Package Legitimacy Gate needed |
</threat_model>

<verification>
Prerequisite (same as every Phase 6-9 verification): the Express backend on :4000 and the Postgres `switera-db-1` container are running; the 3 seed accounts (admin/admin123, manajer/manajer123, logistik/logistik123) exist. Each script preflights `GET /health` and aborts with a clear message if :4000 is unreachable.

1. `node server/src/sync/multiClientSync.verify.mjs` prints `MULTI_CLIENT_SYNC_OK true`, exit 0 — SYNC-01 convergence through the real polling loop, plus the stop-halts-sync check.
2. `node server/src/sync/multiClientRbac.verify.mjs` prints `MULTI_CLIENT_RBAC_OK true`, exit 0 — Success Criterion 3 (403 under polling) with an Admin positive control.
3. `node server/src/sync/multiClientRace.verify.mjs` prints `MULTI_CLIENT_RACE_OK true`, exit 0 — Success Criterion 4 (exactly-one-winner under polling, full client stack).
4. Re-run all three a second time: identical PASS output (idempotency confirmed; no orphaned rows).

HONEST SCOPE (record in SUMMARY): These three scripts simulate two concurrent client sessions in one Node process driving the real store/apiClient against the live backend. They are NOT a rendered two-browser-tab visual proof — no Playwright/chromium-cli is available (carried-forward blocker across Phases 5/9). Success Criterion 1's "without that second session reloading the page" and Success Criterion 2's idle/reopened-tab visual behavior should additionally be confirmed by a MANUAL two-browser-tab check (documented as a manual step in the SUMMARY), not claimed as automated pixel proof.
</verification>

<success_criteria>
- Three idempotent, self-cleaning `*.verify.mjs` scripts under `server/src/sync/` each print their `*_OK true` aggregate line and exit 0.
- SYNC-01 convergence is proven through client B's real poll tick (not a manual fetch); stopPolling is proven to halt sync.
- Phase 7 RBAC 403 and Phase 8 LOGIC-02 exactly-one-winner both re-hold under active polling, driven through two real client sessions.
- SUMMARY honestly records the script-level vs manual-two-tab scope boundary; no false automated-pixel claim.
</success_criteria>

<artifacts_produced>
- `server/src/sync/multiClientSync.verify.mjs` — two-session SYNC-01 convergence proof through the real polling loop + stop-halts-sync check
- `server/src/sync/multiClientRbac.verify.mjs` — RBAC 403-under-polling re-verification with Admin positive control (Success Criterion 3)
- `server/src/sync/multiClientRace.verify.mjs` — LOGIC-02 exactly-one-winner re-verification through two real client sessions under polling (Success Criterion 4)
</artifacts_produced>

<output>
Create `.planning/phases/10-multi-client-sync-cross-role-uat/10-02-SUMMARY.md` when done.
</output>